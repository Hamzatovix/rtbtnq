# Настройка DNS A-записей в REG.RU для rosebotanique.store

## Пошаговая инструкция

### Шаг 1: Вход в панель управления REG.RU

1. Открой браузер и перейди на сайт: https://www.reg.ru/
2. Нажми кнопку **"Войти"** (правый верхний угол)
3. Введи логин и пароль от аккаунта REG.RU
4. Нажми **"Войти"**

---

### Шаг 2: Переход в управление доменом

1. После входа ты увидишь **"Панель управления"**
2. В списке доменов найди **`rosebotanique.store`**
3. Нажми на название домена или на кнопку **"Управление"** рядом с ним

---

### Шаг 3: Открытие раздела DNS

1. В меню управления доменом найди раздел **"DNS-серверы и зона"** или **"DNS"**
2. Нажми на него
3. Ты увидишь список текущих DNS-записей (или пустую таблицу, если записи ещё не настроены)

---

### Шаг 4: Добавление первой A-записи (rosebotanique.store)

1. Найди кнопку **"Добавить запись"** или **"Создать запись"** (обычно вверху или внизу таблицы)
2. Выбери тип записи: **"A"** (из выпадающего списка)
3. Заполни поля:
   - **Поддомен** (или **Имя**): `@` или оставь пустым (это означает корневой домен `rosebotanique.store`)
     - ⚠️ **Важно**: В некоторых панелях нужно указать `@`, в других — оставить пустым. Если не уверен, попробуй сначала `@`
   - **IP-адрес** (или **Значение**): `176.57.213.174`
   - **TTL** (время жизни): оставь по умолчанию (обычно 3600 секунд) или установи `3600`
4. Нажми **"Сохранить"** или **"Добавить"**

---

### Шаг 5: Добавление второй A-записи (www.rosebotanique.store)

1. Снова нажми **"Добавить запись"**
2. Выбери тип: **"A"**
3. Заполни поля:
   - **Поддомен** (или **Имя**): `www`
   - **IP-адрес** (или **Значение**): `176.57.213.174`
   - **TTL**: `3600` (или по умолчанию)
4. Нажми **"Сохранить"** или **"Добавить"**

---

### Шаг 6: Проверка записей

После добавления в таблице должны появиться две записи:

| Тип | Поддомен | Значение | TTL |
|-----|----------|----------|-----|
| A   | @        | 176.57.213.174 | 3600 |
| A   | www      | 176.57.213.174 | 3600 |

---

### Шаг 7: Ожидание распространения DNS

⚠️ **Важно**: Изменения DNS распространяются не мгновенно!

- **Минимальное время**: 5-15 минут
- **Обычное время**: 1-4 часа
- **Максимальное время**: до 24-48 часов

---

## Проверка работы DNS

### Вариант 1: Через командную строку (Windows PowerShell)

```powershell
# Проверка основной записи
nslookup rosebotanique.store

# Проверка www-поддомена
nslookup www.rosebotanique.store
```

**Ожидаемый результат:**
```
Name:    rosebotanique.store
Address:  176.57.213.174
```

### Вариант 2: Через онлайн-сервисы

1. **DNS Checker**: https://dnschecker.org/
   - Введи домен: `rosebotanique.store`
   - Выбери тип записи: `A`
   - Нажми "Search"
   - Проверь, что во всех регионах показывается `176.57.213.174`

2. **What's My DNS**: https://www.whatsmydns.net/
   - Введи домен: `rosebotanique.store`
   - Выбери тип: `A Record`
   - Проверь результаты

### Вариант 3: Через браузер

После распространения DNS попробуй открыть:
- `http://rosebotanique.store` (должен редиректить на HTTPS)
- `https://rosebotanique.store` (должен открыться сайт)

---

## Возможные проблемы и решения

### Проблема 1: "Запись уже существует"

**Решение**: 
- Если запись уже есть, но с другим IP — удали старую и создай новую
- Или отредактируй существующую запись

### Проблема 2: "Не могу найти раздел DNS"

**Решение**:
- В некоторых панелях REG.RU раздел называется **"Управление DNS"** или **"DNS-зона"**
- Или **"Настройки домена"** → **"DNS"**

### Проблема 3: "DNS не обновляется через несколько часов"

**Решение**:
- Проверь, что записи сохранены правильно
- Убедись, что используешь DNS-серверы REG.RU (не внешние)
- Очисти кэш DNS на своём компьютере:
  ```powershell
  ipconfig /flushdns
  ```

### Проблема 4: "Поддомен @ не работает"

**Решение**:
- В некоторых панелях вместо `@` нужно указать пустое значение
- Или попробуй указать полное имя: `rosebotanique.store`

---

## Альтернативный способ (через API REG.RU)

Если у тебя есть API-ключ REG.RU, можно настроить через API, но это сложнее. Рекомендую использовать веб-интерфейс.

---

## После настройки DNS

1. ✅ Дождись распространения DNS (проверь через DNS Checker)
2. ✅ Получи SSL-сертификат через Certbot (см. инструкцию по SSL)
3. ✅ Обнови `NEXT_PUBLIC_BASE_URL` в `.env.production` на `https://rosebotanique.store`
4. ✅ Перезапусти контейнер `rosebotanique`

---

## Скриншоты интерфейса (примерный вид)

### Пример таблицы DNS-записей:
```
┌──────┬──────────────┬──────────────────┬──────┐
│ Тип  │ Поддомен     │ Значение         │ TTL  │
├──────┼──────────────┼──────────────────┼──────┤
│ A    │ @            │ 176.57.213.174   │ 3600 │
│ A    │ www          │ 176.57.213.174   │ 3600 │
└──────┴──────────────┴──────────────────┴──────┘
```

### Пример формы добавления записи:
```
Тип записи: [A ▼]
Поддомен:   [@        ]
IP-адрес:   [176.57.213.174]
TTL:        [3600     ]

[Отмена] [Сохранить]
```

---

## Контакты поддержки REG.RU

Если возникли проблемы:
- **Телефон**: 8 800 200-70-01 (бесплатно по России)
- **Email**: support@reg.ru
- **Чат**: доступен в личном кабинете

---

**Готово! После настройки DNS сайт будет доступен по домену `rosebotanique.store`** 🎉

