# Следующие шаги после настройки DNS

## ✅ Текущий статус

- ✅ Docker контейнер `rosebotanique` работает
- ✅ Nginx настроен и проксирует запросы
- ✅ Сайт доступен по IP: `http://176.57.213.174:3000`
- ✅ HTTPS работает через Nginx (пока без SSL-сертификата)
- ⏳ DNS-записи настроены, ожидается распространение (15-60 минут)

---

## 📋 Чек-лист следующих шагов

### 1. ✅ Проверка DNS (когда распространится)

**Проверка через PowerShell:**
```powershell
nslookup rosebotanique.store
nslookup www.rosebotanique.store
```

**Ожидаемый результат:**
```
Address: 176.57.213.174
```

**Онлайн-проверка:**
- https://dnschecker.org/ — введи `rosebotanique.store`, тип `A`, должно быть `176.57.213.174` везде

---

### 2. 🔒 Получение SSL-сертификата (Let's Encrypt)

**Важно:** SSL-сертификат можно получить только после того, как DNS распространится!

#### Шаг 2.1: Подключение к серверу

```bash
ssh root@176.57.213.174
```

#### Шаг 2.2: Установка Certbot

```bash
# Обновление пакетов
apt update

# Установка Certbot
apt install -y certbot python3-certbot-nginx
```

#### Шаг 2.3: Получение сертификата

```bash
# Получение сертификата для обоих доменов
certbot certonly --nginx \
  -d rosebotanique.store \
  -d www.rosebotanique.store
```

**Что произойдёт:**
- Certbot проверит DNS (должен быть настроен!)
- Создаст временные файлы для проверки домена
- Выдаст SSL-сертификаты
- Сохранит их в `/etc/letsencrypt/live/rosebotanique.store/`

#### Шаг 2.4: Настройка Nginx для использования сертификата

**Вариант 1: Автоматическая настройка (Certbot сделает сам)**

Certbot может автоматически настроить Nginx, но так как у нас Nginx в Docker, нужно сделать вручную.

**Вариант 2: Ручная настройка**

1. Скопируй сертификаты в директорию, доступную для Docker:

```bash
# Создай директории для сертификатов
mkdir -p /root/Shtraf/nginx/ssl/rosebotanique

# Скопируй сертификаты
cp /etc/letsencrypt/live/rosebotanique.store/fullchain.pem /root/Shtraf/nginx/ssl/rosebotanique/certificate.crt
cp /etc/letsencrypt/live/rosebotanique.store/privkey.pem /root/Shtraf/nginx/ssl/rosebotanique/certificate.key

# Установи правильные права
chmod 644 /root/Shtraf/nginx/ssl/rosebotanique/certificate.crt
chmod 600 /root/Shtraf/nginx/ssl/rosebotanique/certificate.key
```

2. Обнови `docker-compose.yml` для монтирования сертификатов:

```bash
cd /root/Shtraf
nano docker-compose.yml
```

Добавь в секцию `volumes` для `nginx`:
```yaml
volumes:
  - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro
  - ./nginx/certificate.crt:/etc/ssl/certs/shtraftest.ru.crt:ro
  - ./nginx/certificate.key:/etc/ssl/private/shtraftest.ru.key:ro
  # Добавь эти строки:
  - ./nginx/ssl/rosebotanique/certificate.crt:/etc/ssl/certs/rosebotanique.store.crt:ro
  - ./nginx/ssl/rosebotanique/certificate.key:/etc/ssl/private/rosebotanique.store.key:ro
```

3. Раскомментируй SSL-директивы в `nginx.conf`:

```bash
nano /root/Shtraf/nginx/nginx.conf
```

Найди блок для `rosebotanique.store` и раскомментируй:
```nginx
# Было (закомментировано):
# ssl_certificate     /etc/ssl/certs/rosebotanique.store.crt;
# ssl_certificate_key /etc/ssl/private/rosebotanique.store.key;

# Должно быть (раскомментировано):
ssl_certificate     /etc/ssl/certs/rosebotanique.store.crt;
ssl_certificate_key /etc/ssl/private/rosebotanique.store.key;
ssl_protocols TLSv1.2 TLSv1.3;
ssl_ciphers 'EECDH+AESGCM:EECDH+CHACHA20:EDH+AESGCM';
ssl_prefer_server_ciphers off;
ssl_session_cache shared:SSL:10m;
ssl_session_timeout 1d;
ssl_session_tickets off;
```

4. Перезапусти Nginx:

```bash
cd /root/Shtraf
docker compose restart nginx
docker exec shtrafnett_nginx nginx -s reload
```

5. Проверь работу:

```bash
curl -I https://rosebotanique.store -k
```

#### Шаг 2.5: Автоматическое обновление сертификата

Let's Encrypt сертификаты действуют 90 дней. Настрой автоматическое обновление:

```bash
# Проверка автоматического обновления (Certbot создаёт cron-задачу автоматически)
certbot renew --dry-run
```

---

### 3. 🔧 Обновление переменных окружения

#### Шаг 3.1: Обновление .env.production

```bash
# На сервере
cd /opt/rosebotanique
nano front/.env.production
```

Убедись, что указано:
```env
NEXT_PUBLIC_BASE_URL=https://rosebotanique.store
```

**Важно:** Используй `https://`, а не `http://`!

#### Шаг 3.2: Перезапуск контейнера

```bash
# Останови контейнер
docker stop rosebotanique

# Запусти заново (он подхватит новые переменные)
docker start rosebotanique

# Или пересобери, если нужно
cd /opt/rosebotanique
docker build -t rosebotanique .
docker run -d \
  --name rosebotanique \
  --restart unless-stopped \
  -p 3000:3000 \
  -v $(pwd)/front/prisma_data:/app/prisma_data \
  --env-file front/.env.production \
  rosebotanique
```

---

### 4. ✅ Финальная проверка

#### Проверка 1: Доступность по домену

```bash
# HTTP (должен редиректить на HTTPS)
curl -I http://rosebotanique.store

# HTTPS (должен возвращать 200)
curl -I https://rosebotanique.store
```

#### Проверка 2: SSL-сертификат

Открой в браузере: `https://rosebotanique.store`

- Должен быть зелёный замочек 🔒
- Не должно быть предупреждений о небезопасном соединении

#### Проверка 3: Работа сайта

- Открой главную страницу
- Проверь работу каталога товаров
- Проверь работу корзины
- Проверь работу оформления заказа

---

## 🎯 Приоритет действий

### Сейчас (можно делать сразу):

1. ✅ Проверь DNS через онлайн-сервис: https://dnschecker.org/
2. ✅ Обнови `NEXT_PUBLIC_BASE_URL` в `.env.production`
3. ✅ Перезапусти контейнер `rosebotanique`

### После распространения DNS (через 15-60 минут):

4. ✅ Получи SSL-сертификат через Certbot
5. ✅ Настрой Nginx для использования сертификата
6. ✅ Проверь работу сайта по домену

---

## 🐛 Возможные проблемы

### Проблема: Certbot не может получить сертификат

**Причины:**
- DNS ещё не распространился
- Домен не указывает на правильный IP
- Порт 80 недоступен извне (но у тебя он доступен через Nginx)

**Решение:**
- Подожди ещё 30-60 минут
- Проверь DNS через https://dnschecker.org/
- Убедись, что домен указывает на `176.57.213.174`

### Проблема: Сайт не открывается по домену

**Причины:**
- DNS ещё не распространился
- Неправильные DNS-записи

**Решение:**
- Проверь DNS через `nslookup` или онлайн-сервис
- Убедись, что A-записи указывают на `176.57.213.174`

### Проблема: SSL-сертификат не работает

**Причины:**
- Неправильные пути к сертификатам в Nginx
- Сертификаты не смонтированы в Docker

**Решение:**
- Проверь пути в `docker-compose.yml`
- Убедись, что файлы существуют: `ls -la /root/Shtraf/nginx/ssl/rosebotanique/`
- Проверь логи Nginx: `docker logs shtrafnett_nginx`

---

## 📞 Полезные команды для диагностики

```bash
# Проверка DNS на сервере
nslookup rosebotanique.store
dig rosebotanique.store

# Проверка доступности сайта
curl -I http://rosebotanique.store
curl -I https://rosebotanique.store

# Проверка SSL-сертификата
openssl s_client -connect rosebotanique.store:443 -servername rosebotanique.store

# Логи Nginx
docker logs shtrafnett_nginx

# Логи приложения
docker logs rosebotanique

# Проверка портов
netstat -tulpn | grep -E ':(80|443|3000)'
```

---

**Готово! Следуй этим шагам по порядку** 🚀

