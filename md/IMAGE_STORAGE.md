# 📸 Хранение фотографий

## 🎯 Куда отправляются фотографии?

Фотографии, загружаемые через админ панель (бекофис), сохраняются в **Vercel Blob Storage**.

## 📍 Детали хранения

### Сервис хранения
- **Vercel Blob Storage** - облачное хранилище файлов от Vercel
- Используется библиотека `@vercel/blob`
- Файлы доступны публично через CDN

### API эндпоинт
- **Путь**: `/api/upload/image`
- **Метод**: `POST`
- **Файл**: `front/src/app/api/upload/image/route.ts`

### Процесс загрузки

1. **Клиент отправляет файл**:
   ```typescript
   const formData = new FormData()
   formData.append('file', file)
   formData.append('folder', 'gallery') // или 'products'
   ```

2. **API обрабатывает файл**:
   - Проверяет тип файла (должен быть изображением)
   - Проверяет размер (максимум 5MB)
   - Генерирует уникальное имя файла: `${Date.now()}-${nanoid(10)}.${ext}`
   - Сохраняет в папку: `folder/${filename}`

3. **Файл сохраняется в Vercel Blob**:
   ```typescript
   const { url } = await put(`${folder}/${filename}`, Buffer.from(arrayBuffer), {
     access: 'public',
     contentType: file.type,
   })
   ```

4. **Возвращается публичный URL**:
   - Формат: `https://*.public.blob.vercel-storage.com/folder/filename`
   - URL сохраняется в базе данных (Supabase)

## 📁 Структура папок

### Галерея (`/backoffice/gallery`)
- **Папка**: `gallery/`
- **Пример URL**: `https://*.public.blob.vercel-storage.com/gallery/1763717251351-UyY1Uz18Rj.png`
- **Метаданные**: Сохраняются в таблице `gallery` в Supabase

### Товары (`/backoffice/products`)
- **Папка**: `products/` (по умолчанию)
- **Пример URL**: `https://*.public.blob.vercel-storage.com/products/1763717251351-abc123.jpg`
- **Метаданные**: Сохраняются в таблице `Product` в базе данных

## 🔧 Технические детали

### Ограничения
- **Максимальный размер файла**: 5MB
- **Типы файлов**: Только изображения (`image/*`)
- **Доступ**: Публичный (`access: 'public'`)

### Именование файлов
- Формат: `{timestamp}-{randomId}.{extension}`
- Пример: `1763717251351-UyY1Uz18Rj.png`
- Гарантирует уникальность имен

### Конфигурация Next.js
В `next.config.js` настроены `remotePatterns` для загрузки изображений:
```javascript
remotePatterns: [
  {
    protocol: 'https',
    hostname: '*.public.blob.vercel-storage.com',
    pathname: '/products/**',
  },
  {
    protocol: 'https',
    hostname: '*.public.blob.vercel-storage.com',
    pathname: '/gallery/**',
  },
]
```

## 📊 Поток данных

```
Админ панель (бекофис)
    ↓
[Выбор файла]
    ↓
[POST /api/upload/image]
    ↓
[Валидация файла]
    ↓
[Генерация имени файла]
    ↓
[Загрузка в Vercel Blob Storage]
    ↓
[Получение публичного URL]
    ↓
[Сохранение URL в Supabase/БД]
    ↓
[Отображение в интерфейсе]
```

## 🔐 Безопасность

1. **Валидация на сервере**:
   - Проверка типа файла
   - Проверка размера файла
   - Защита от вредоносных файлов

2. **Публичный доступ**:
   - Файлы доступны публично через CDN
   - Это нормально для изображений галереи и товаров
   - Для приватных файлов нужно использовать `access: 'private'`

## 💾 Где хранятся метаданные?

### Галерея
- **База данных**: Supabase (таблица `gallery`)
- **Поля**: `id`, `src` (URL), `alt`, `data` (JSONB), `created_at`, `updated_at`

### Товары
- **База данных**: PostgreSQL через Prisma
- **Таблица**: `Product`, `ProductVariant`
- **Поля**: `images` (массив URL)

## 🚀 Преимущества Vercel Blob Storage

1. **CDN**: Быстрая доставка изображений по всему миру
2. **Масштабируемость**: Автоматическое масштабирование
3. **Интеграция**: Тесная интеграция с Vercel
4. **Простота**: Простой API для загрузки файлов
5. **Производительность**: Оптимизированная доставка контента

## 📝 Примеры использования

### Загрузка в галерею
```typescript
const formData = new FormData()
formData.append('file', file)
formData.append('folder', 'gallery')

const res = await fetch('/api/upload/image', {
  method: 'POST',
  body: formData,
})
const { url } = await res.json()
// url: https://*.public.blob.vercel-storage.com/gallery/1763717251351-abc123.png
```

### Загрузка для товара
```typescript
const formData = new FormData()
formData.append('file', file)
// folder по умолчанию 'products'

const res = await fetch('/api/upload/image', {
  method: 'POST',
  body: formData,
})
const { url } = await res.json()
// url: https://*.public.blob.vercel-storage.com/products/1763717251351-abc123.jpg
```

## 🔍 Проверка загруженных файлов

### В Vercel Dashboard
1. Перейдите в проект на Vercel
2. Откройте раздел "Storage"
3. Выберите "Blob Storage"
4. Просмотрите загруженные файлы

### В коде
- URL файла сохраняется в базе данных
- Можно проверить через Supabase Dashboard (для галереи)
- Можно проверить через Prisma Studio (для товаров)

## ⚠️ Важные замечания

1. **Удаление файлов**: При удалении записи из базы данных, файл в Blob Storage не удаляется автоматически. Нужно добавить логику удаления файлов.

2. **Резервное копирование**: Рекомендуется настроить резервное копирование важных изображений.

3. **Ограничения Vercel**: Проверьте лимиты хранилища в вашем плане Vercel.

4. **Миграция**: Если нужно сменить хранилище, потребуется миграция всех URL в базе данных.

## 📚 Дополнительная информация

- **Vercel Blob Storage**: https://vercel.com/docs/storage/vercel-blob
- **API документация**: https://vercel.com/docs/storage/vercel-blob/using-blob-sdk
- **Лимиты**: Зависит от вашего плана Vercel

