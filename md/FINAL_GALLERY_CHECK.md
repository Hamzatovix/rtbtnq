# ✅ Финальная проверка работы галереи

## 🎯 Чеклист успешной настройки

### 1. База данных Supabase
- [x] Таблица `gallery` создана
- [x] Структура таблицы правильная (id, src, alt, data, created_at, updated_at)
- [x] RLS отключен или настроены политики

### 2. Переменные окружения в Vercel
- [x] `SUPABASE_URL` установлена
- [x] `SUPABASE_SERVICE_ROLE_KEY` установлена
- [x] Переменные применены к Production

### 3. Код обновлен
- [x] Новый деплой выполнен на Vercel
- [x] Код использует Supabase вместо файлов на Vercel

## ✅ Проверка работы

### Тест 1: Добавление изображения

1. Откройте ваше приложение на Vercel
2. Перейдите в `/backoffice/gallery`
3. Нажмите "Добавить изображение"
4. Выберите файл изображения
5. ✅ Изображение должно загрузиться и появиться в списке
6. ✅ **НЕ должно быть ошибки** `EROFS` или `relation "gallery" does not exist`

### Тест 2: Проверка в Supabase

1. Откройте Supabase Dashboard
2. Перейдите в **Table Editor** → `gallery`
3. ✅ Должна появиться новая запись с данными изображения:
   - `id` - уникальный идентификатор
   - `src` - URL изображения
   - `alt` - описание изображения
   - `data` - JSON объект с данными

### Тест 3: Редактирование alt текста

1. В админ панели галереи отредактируйте alt текст изображения
2. Нажмите вне поля (onBlur)
3. ✅ Изменения должны сохраниться
4. ✅ В Supabase таблице `gallery` должен обновиться `alt` и `updated_at`

### Тест 4: Удаление изображения

1. В админ панели нажмите кнопку удаления (иконка корзины)
2. Подтвердите удаление
3. ✅ Изображение должно исчезнуть из списка
4. ✅ Запись должна удалиться из Supabase таблицы `gallery`

### Тест 5: Публичная страница

1. Откройте `/gallery` на вашем сайте
2. ✅ Должна отображаться карусель с изображениями
3. ✅ Изображения должны загружаться корректно

## 🔍 Проверка логов Vercel

После выполнения тестов проверьте логи в Vercel:

**Vercel Dashboard → Deployments → [ваш деплой] → Functions → Logs**

Должны быть логи:
```
[Gallery] Сохранение галереи: {
  isVercel: true,
  supabaseEnabled: true,  ← должно быть true
  imagesCount: 1,
  hasSupabaseUrl: true,
  hasSupabaseKey: true
}
[Gallery] Используем Supabase для сохранения
```

**НЕ должно быть:**
- ❌ `EROFS: read-only file system`
- ❌ `relation "gallery" does not exist`
- ❌ `401 Unauthorized`
- ❌ `404 Not Found`

## ✅ Если все тесты пройдены

**Поздравляем! 🎉**

Галерея полностью настроена и работает:
- ✅ Данные сохраняются в Supabase
- ✅ Нет ошибок при работе на Vercel
- ✅ Все функции работают корректно
- ✅ Публичная страница отображает галерею

## 🐛 Если что-то не работает

### Проблема: Изображение не добавляется

**Проверьте:**
1. Логи Vercel - есть ли ошибки?
2. Supabase таблица `gallery` - создана ли?
3. Переменные окружения - правильные ли?

### Проблема: Ошибка при сохранении

**Проверьте логи:**
- Если `supabaseEnabled: false` → проверьте переменные окружения
- Если `401 Unauthorized` → проверьте `SUPABASE_SERVICE_ROLE_KEY`
- Если `404 Not Found` → проверьте, что таблица `gallery` существует

### Проблема: Данные не отображаются на публичной странице

**Проверьте:**
1. API `/api/gallery` возвращает данные?
2. Откройте в браузере: `https://ваш-домен.vercel.app/api/gallery`
3. Должен вернуться JSON с массивом `images`

## 📚 Дополнительная документация

- `md/GALLERY_SUPABASE_SETUP.md` - полная инструкция по настройке
- `md/GALLERY_CHECK.md` - проверка системы
- `md/GALLERY_VERIFICATION.md` - детальная проверка
- `md/CREATE_GALLERY_TABLE.md` - создание таблицы

