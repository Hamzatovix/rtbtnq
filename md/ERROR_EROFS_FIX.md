# 🔧 Исправление ошибки EROFS на Vercel

## ❌ Ошибка

```
Ошибка при сохранении галереи: Error: EROFS: read-only file system, 
open '/var/task/front/src/data/gallery.json'
```

## 🔍 Что это значит?

**EROFS** = **E**rror **R**ead-**O**nly **F**ile **S**ystem

На Vercel (и других serverless платформах) файловая система доступна **только для чтения**. Код пытается записать в файл `gallery.json`, но это невозможно.

## ✅ Решение

Нужно настроить **Supabase** для хранения данных вместо файлов.

### Шаг 1: Создать таблицу в Supabase

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **SQL Editor**
4. Выполните этот SQL:

```sql
CREATE TABLE IF NOT EXISTS gallery (
  id TEXT PRIMARY KEY,
  src TEXT NOT NULL,
  alt TEXT DEFAULT '',
  data JSONB,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_gallery_created_at ON gallery(created_at);
```

### Шаг 2: Получить данные для переменных окружения

В Supabase Dashboard:

1. Перейдите в **Settings** → **API**
2. Найдите:
   - **Project URL** → это ваш `SUPABASE_URL`
   - **service_role key** → это ваш `SUPABASE_SERVICE_ROLE_KEY` (секретный ключ!)

### Шаг 3: Установить переменные окружения в Vercel

1. Откройте ваш проект в [Vercel Dashboard](https://vercel.com)
2. Перейдите в **Settings** → **Environment Variables**
3. Добавьте следующие переменные:

```
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-ключ
```

**Важно:**
- Не используйте `anon` key, только `service_role` key!
- Не добавляйте кавычки вокруг значений
- Убедитесь, что переменные применены к **Production** окружению

### Шаг 4: Перезапустить приложение

После добавления переменных:

1. В Vercel перейдите в **Deployments**
2. Найдите последний деплой
3. Нажмите **Redeploy** (или сделайте новый commit и push)

### Шаг 5: Проверить работу

1. Откройте ваше приложение на Vercel
2. Перейдите в `/backoffice/gallery`
3. Попробуйте добавить изображение
4. ✅ Ошибка должна исчезнуть!

## 🔍 Как проверить, что Supabase настроен правильно?

### Проверка 1: Логи Vercel

После настройки в логах **не должно быть** ошибки `EROFS`.

### Проверка 2: Supabase Dashboard

1. Откройте Supabase Dashboard
2. Перейдите в **Table Editor** → **gallery**
3. После добавления изображения должна появиться новая запись

### Проверка 3: API

Откройте в браузере: `https://ваш-домен.vercel.app/api/gallery`

Должен вернуться JSON с массивом изображений (может быть пустым, если галерея пуста).

## 🐛 Если ошибка не исчезла

### Проблема: Переменные окружения не применяются

**Решение:**
1. Убедитесь, что переменные добавлены для **Production**
2. Перезапустите деплой (Redeploy)
3. Проверьте, что названия переменных точные (без опечаток):
   - `SUPABASE_URL` (не `SUPABASE_URLS` или `SUPABASE_URI`)
   - `SUPABASE_SERVICE_ROLE_KEY` (не `SUPABASE_KEY`)

### Проблема: "401 Unauthorized"

**Причина:** Используется неправильный ключ

**Решение:**
1. Убедитесь, что используете **service_role** key, а не **anon** key
2. Скопируйте ключ заново из Supabase Dashboard → Settings → API

### Проблема: "Table does not exist"

**Причина:** Таблица не создана

**Решение:**
1. Выполните SQL скрипт из Шага 1
2. Проверьте в Table Editor, что таблица `gallery` существует

## 📝 Быстрая справка

**Файл с SQL миграцией:** `front/supabase/migrations/create_gallery_table.sql`

**Подробная документация:** `md/GALLERY_SUPABASE_SETUP.md`

**Проверка системы:** `md/GALLERY_CHECK.md`

## ✅ После настройки

После правильной настройки Supabase:
- ✅ Данные сохраняются в Supabase таблице `gallery`
- ✅ Ошибка `EROFS` больше не появляется
- ✅ Галерея работает на Vercel
- ✅ Локально можно продолжать использовать файл (если Supabase не настроен локально)

