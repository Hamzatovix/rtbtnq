# 🔍 Проверка работы Telegram уведомлений при заказе

## ✅ Быстрая проверка

### 1. Проверка переменных окружения

Убедитесь, что в файле `front/.env.local` или `front/.env` установлены:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id
```

**Важно:** 
- Переменные должны быть без кавычек
- Для группы Chat ID должен быть отрицательным числом (например: `-1001234567890`)
- Для личного чата Chat ID - положительное число (например: `123456789`)

### 2. Проверка в консоли сервера

При создании заказа в консоли должны появиться логи:

```
[Order] Начало отправки уведомления в Telegram...
[Telegram] Проверка конфигурации: { hasToken: true, hasChatId: true, ... }
[Telegram] Отправка уведомления о заказе: { orderNumber: '...', ... }
[Telegram] Сообщение успешно отправлено в Telegram
[Order] Результат отправки в Telegram: успешно
```

### 3. Возможные проблемы и решения

#### ❌ Проблема: "Telegram bot token or chat ID not configured"

**Решение:**
1. Проверьте наличие файла `.env.local` в папке `front/`
2. Убедитесь, что переменные названы правильно (без опечаток)
3. Перезапустите сервер Next.js после изменения `.env.local`

#### ❌ Проблема: Заказ создается, но уведомление не приходит

**Проверьте:**
1. **Логи в консоли** - должны быть сообщения `[Telegram]`
2. **Токен бота** - проверьте через:
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getMe
   ```
   Должен вернуть информацию о боте
3. **Chat ID** - проверьте через:
   ```
   https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates
   ```
   Найдите ваш Chat ID в ответе

#### ❌ Проблема: "Bad Request" или "Unauthorized"

**Возможные причины:**
- Неправильный токен бота
- Неправильный Chat ID
- Бот не добавлен в группу (если используется группа)
- Бот заблокирован

**Решение:**
1. Пересоздайте токен через @BotFather (`/token`)
2. Проверьте Chat ID через `getUpdates`
3. Убедитесь, что бот добавлен в группу и имеет права на отправку сообщений

### 4. Ручная проверка через API

Выполните в браузере или через curl:

```bash
# Проверка бота
curl "https://api.telegram.org/bot<ВАШ_ТОКЕН>/getMe"

# Отправка тестового сообщения
curl -X POST "https://api.telegram.org/bot<ВАШ_ТОКЕН>/sendMessage" \
  -H "Content-Type: application/json" \
  -d '{
    "chat_id": "<ВАШ_CHAT_ID>",
    "text": "Тестовое сообщение"
  }'
```

Если тестовое сообщение приходит, значит проблема в коде приложения.

### 5. Проверка кода

Код отправки уведомлений находится в:
- `front/src/lib/telegram.ts` - функции отправки
- `front/src/app/api/orders/route.ts` - вызов при создании заказа
- `front/src/server/orders/orders-json.service.ts` - альтернативный вызов

**Важно:** Уведомления отправляются асинхронно и не блокируют создание заказа. Если отправка не удалась, заказ все равно будет создан.

### 6. Логи для отладки

Включите подробные логи, добавив в код:

```typescript
console.log('[Telegram Debug]', {
  token: process.env.TELEGRAM_BOT_TOKEN ? 'установлен' : 'не установлен',
  chatId: process.env.TELEGRAM_CHAT_ID,
  orderData: data
})
```

### 7. Проверка на production

На production (Vercel/другой хостинг):
1. Убедитесь, что переменные окружения установлены в настройках проекта
2. Проверьте, что `NEXT_PUBLIC_BASE_URL` указывает на правильный домен
3. Для localhost изображения товаров не отправляются (это нормально)

## 📋 Чеклист проверки

- [ ] Переменные `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` установлены
- [ ] Сервер Next.js перезапущен после изменения переменных
- [ ] Бот создан через @BotFather
- [ ] Chat ID получен и правильный
- [ ] Бот добавлен в группу (если используется группа)
- [ ] Тестовое сообщение через API приходит
- [ ] В консоли есть логи `[Telegram]`
- [ ] Уведомление приходит в Telegram при создании заказа

## 🆘 Если ничего не помогает

1. Проверьте документацию: `md/TELEGRAM_SETUP.md`
2. Проверьте примеры: `md/TELEGRAM_QUICK_SETUP.md`
3. Проверьте логи сервера на наличие ошибок
4. Убедитесь, что используете правильный формат токена и Chat ID


