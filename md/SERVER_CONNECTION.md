# 🔐 Подключение к серверу через SSH

## Данные для подключения

- **Хост:** 193.160.209.34
- **Порт:** 22
- **Пользователь:** root
- **Пароль:** `oBMGz^RTq2rn,o`

## Способ 1: SSH через PowerShell (Windows)

### Базовое подключение

```powershell
ssh root@193.160.209.34 -p 22
```

После выполнения команды система запросит пароль. Введите:
```
oBMGz^RTq2rn,o
```

**Важно:** При вводе пароля символы не отображаются (это нормально для безопасности).

### Подключение с явным указанием пароля (не рекомендуется для безопасности)

SSH не поддерживает передачу пароля через командную строку напрямую. Используйте один из методов ниже.

## Способ 2: Использование sshpass (если установлен)

Если у вас установлен `sshpass`:

```powershell
sshpass -p 'oBMGz^RTq2rn,o' ssh root@193.160.209.34 -p 22
```

**Примечание:** `sshpass` обычно не входит в стандартную установку Windows.

## Способ 3: Использование PuTTY (Windows)

1. Скачайте и установите [PuTTY](https://www.putty.org/)
2. Откройте PuTTY
3. Заполните поля:
   - **Host Name (or IP address):** `193.160.209.34`
   - **Port:** `22`
   - **Connection type:** SSH
4. Нажмите **Open**
5. Введите логин: `root`
6. Введите пароль: `oBMGz^RTq2rn,o`

## Способ 4: Использование WinSCP (для SFTP/SCP)

1. Скачайте и установите [WinSCP](https://winscp.net/)
2. Откройте WinSCP
3. Заполните:
   - **File protocol:** SFTP
   - **Host name:** `193.160.209.34`
   - **Port number:** `22`
   - **User name:** `root`
   - **Password:** `oBMGz^RTq2rn,o`
4. Нажмите **Login**

## Способ 5: Использование PowerShell с сохранением сессии

### Создание SSH-ключа (рекомендуется для безопасности)

```powershell
# Генерация SSH-ключа (если еще нет)
ssh-keygen -t rsa -b 4096

# Копирование ключа на сервер (потребует ввода пароля один раз)
ssh-copy-id root@193.160.209.34 -p 22
```

После этого можно подключаться без пароля:
```powershell
ssh root@193.160.209.34 -p 22
```

## Способ 6: Использование Windows Terminal с профилем

Создайте профиль в Windows Terminal для быстрого доступа:

1. Откройте Windows Terminal
2. Нажмите `Ctrl + ,` (настройки)
3. Добавьте новый профиль:

```json
{
    "name": "Production Server",
    "commandline": "ssh root@193.160.209.34 -p 22",
    "icon": "ms-appx:///ProfileIcons/{9acb9455-ca41-5af7-950f-6bca1b6b5f00}.png"
}
```

## Проверка доступности сервера

### Способ 1: Ping (проверка сетевой доступности)

```powershell
ping -n 4 193.160.209.34
```

**Что проверяет:** Доступен ли сервер по сети (ICMP протокол)

**Ожидаемый результат:**
```
Ответ от 193.160.209.34: число байт=32 время=124мс TTL=57
...
Статистика Ping для 193.160.209.34:
    Пакетов: отправлено = 4, получено = 4, потеряно = 0
    (0% потерь)
```

✅ **Если ping успешен** - сервер доступен по сети  
❌ **Если ping не проходит** - сервер недоступен или файрвол блокирует ICMP

### Способ 2: Проверка порта SSH (22)

```powershell
Test-NetConnection -ComputerName 193.160.209.34 -Port 22
```

**Что проверяет:** Открыт ли порт 22 для SSH подключения

**Ожидаемый результат (успех):**
```
ComputerName           : 193.160.209.34
RemoteAddress          : 193.160.209.34
RemotePort             : 22
TcpTestSucceeded       : True  ← Это должно быть True
```

**Текущий результат (порт закрыт):**
```
PingSucceeded          : True
TcpTestSucceeded       : False  ← Порт недоступен
```

### Способ 3: Проверка нескольких портов одновременно

```powershell
# Проверка SSH порта
Test-NetConnection -ComputerName 193.160.209.34 -Port 22

# Проверка HTTP порта
Test-NetConnection -ComputerName 193.160.209.34 -Port 80

# Проверка HTTPS порта
Test-NetConnection -ComputerName 193.160.209.34 -Port 443
```

### Способ 4: Использование telnet (если установлен)

```powershell
telnet 193.160.209.34 22
```

Если подключение успешно - увидите приглашение SSH или сообщение об ошибке подключения.

### Способ 5: Использование nmap (если установлен)

```powershell
nmap -p 22 193.160.209.34
```

Покажет статус порта: `open`, `closed` или `filtered`

## Решение проблем

### ❌ Проблема: "Connection timed out" или порт недоступен

**Текущая ситуация:**
- ✅ Ping успешен - сервер доступен по сети
- ❌ Порт 22 недоступен - TCP подключение не удается
- ❌ Все проверенные порты (22, 80, 443, 2222, 22022) недоступны

**Возможные причины:**

#### 1. 🔴 Сервер не оплачен (наиболее вероятно в вашем случае)

**Симптомы:**
- ✅ Ping проходит успешно (сервер физически доступен)
- ❌ Все порты недоступны (SSH, HTTP, HTTPS)
- ❌ TCP подключения таймаутят

**Что происходит:**
Провайдер хостинга может:
- Оставить сервер включенным, но заблокировать все сетевые порты
- Разрешить только ICMP (ping) для проверки доступности
- Заблокировать SSH, HTTP и другие сервисы до оплаты

**Решение:**
1. **Проверьте статус оплаты** в панели управления хостингом
2. **Оплатите сервер**, если платеж просрочен
3. **Дождитесь активации** (обычно 5-30 минут после оплаты)
4. **Повторите проверку** после оплаты

#### 2. Файрвол блокирует порты

**Решение:**
- Обратитесь к администратору сервера для проверки файрвола
- Проверьте настройки файрвола в панели управления хостингом
- Убедитесь, что порт 22 открыт для входящих подключений

#### 3. SSH-сервис не запущен

**Решение:**
- Проверьте статус SSH-сервиса (требует доступа к серверу)
- Обратитесь к администратору для запуска SSH

#### 4. Используется другой порт для SSH

**Решение:**
- Уточните правильный номер порта SSH у администратора
- Проверьте альтернативные порты:
   ```powershell
   Test-NetConnection -ComputerName 193.160.209.34 -Port 2222
   Test-NetConnection -ComputerName 193.160.209.34 -Port 22022
   ```

#### 5. Требуется VPN подключение

**Решение:**
- Подключитесь к VPN, если он требуется для доступа к серверу
- Повторите проверку после подключения к VPN

**Диагностика:**
1. **Проверьте доступность сервера:**
   ```powershell
   ping -n 4 193.160.209.34
   ```

2. **Проверьте порт SSH:**
   ```powershell
   Test-NetConnection -ComputerName 193.160.209.34 -Port 22
   ```

3. **Если ping работает, но все порты недоступны:**
   - ⚠️ **В первую очередь проверьте статус оплаты сервера**
   - Проверьте панель управления хостингом на наличие уведомлений
   - Обратитесь в службу поддержки хостинг-провайдера

### ❌ Проблема: "Permission denied"

**Возможные причины:**
- Неправильный пароль
- Неправильное имя пользователя
- SSH-сервис не разрешает вход по паролю

**Решение:**
1. Проверьте правильность пароля (обратите внимание на специальные символы)
2. Убедитесь, что используете правильное имя пользователя (`root`)

### ❌ Проблема: "Host key verification failed"

**Решение:**
Удалите старый ключ хоста:
```powershell
ssh-keygen -R 193.160.209.34
```

## Безопасность

⚠️ **Важные рекомендации:**

1. **Не храните пароли в скриптах** - используйте SSH-ключи
2. **Используйте SSH-ключи вместо паролей** для автоматизации
3. **Ограничьте доступ по IP** на сервере, если возможно
4. **Используйте нестандартный порт SSH** для дополнительной безопасности
5. **Отключите вход root по паролю** после настройки SSH-ключей

## Примеры команд после подключения

После успешного подключения вы можете выполнять команды:

```bash
# Проверка текущей директории
pwd

# Список файлов
ls -la

# Проверка использования диска
df -h

# Проверка процессов
ps aux

# Выход из сессии
exit
```

## Автоматизация подключения

### Создание скрипта для быстрого подключения

Создайте файл `connect-server.ps1`:

```powershell
# connect-server.ps1
$hostname = "193.160.209.34"
$port = 22
$username = "root"

Write-Host "Подключение к серверу $hostname..." -ForegroundColor Green
ssh $username@$hostname -p $port
```

Запуск:
```powershell
.\connect-server.ps1
```

