# 🔍 Диагностика проблемы синхронизации галереи

## ❌ Проблема

Загруженные фотографии не видны при заходе с другого браузера.

## 🔍 Вероятные причины

### 1. Данные сохраняются локально, а не в Supabase

**Симптомы:**
- Фотографии видны только в том браузере, где были загружены
- При заходе с другого браузера/устройства галерея пуста
- Локально фотографии отображаются, на production - нет

**Причина:**
- При локальной загрузке через админ панель, если Supabase не настроен локально, данные сохраняются в локальный файл `front/src/data/gallery.json`
- Этот файл существует только на вашем компьютере
- На production данные загружаются из Supabase, но там их нет

**Решение:**
1. Настройте Supabase локально (создайте `front/.env.local`):
   ```env
   SUPABASE_URL=https://your-project.supabase.co
   SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
   ```

2. Перезапустите локальный сервер после добавления переменных

3. Загрузите фотографии заново - они сохранятся в Supabase

### 2. Переменные окружения Supabase не настроены в Vercel

**Симптомы:**
- На production галерея пуста
- В логах Vercel ошибки о Supabase

**Решение:**
1. Vercel Dashboard → Settings → Environment Variables
2. Убедитесь, что установлены:
   - `SUPABASE_URL`
   - `SUPABASE_SERVICE_ROLE_KEY`
3. Перезапустите деплой (Redeploy)

### 3. Данные не сохраняются в Supabase из-за ошибки

**Симптомы:**
- При загрузке фотографии появляется ошибка
- В консоли браузера ошибка сохранения

**Решение:**
1. Проверьте логи Vercel для деталей ошибки
2. Проверьте таблицу `gallery` в Supabase Dashboard
3. Убедитесь, что Service Role Key имеет права на запись

## 🔧 Как проверить, куда сохраняются данные

### Проверка 1: Логи при сохранении

При загрузке фотографии проверьте логи:

**Локально:**
- Откройте терминал где запущен `npm run dev`
- Ищите сообщения `[Gallery] Сохранение галереи:`
- Проверьте значение `supabaseEnabled`

**На Production:**
- Vercel Dashboard → Deployments → [Latest] → Functions → `/api/gallery`
- Ищите логи `[Gallery API]` и `[Gallery]`

### Проверка 2: Проверка переменных окружения

**Локально:**
```bash
# В папке front/
cat .env.local | grep SUPABASE
```

**На Production:**
- Vercel Dashboard → Settings → Environment Variables
- Проверьте наличие `SUPABASE_URL` и `SUPABASE_SERVICE_ROLE_KEY`

### Проверка 3: Проверка данных в Supabase

Откройте Supabase Dashboard → Table Editor → `gallery`:

```sql
SELECT COUNT(*) FROM gallery;
SELECT * FROM gallery ORDER BY created_at DESC LIMIT 5;
```

Если таблица пуста - данные не сохраняются в Supabase.

## ✅ Правильная настройка

### Для синхронизации между устройствами:

1. **Локально** - создайте `front/.env.local`:
   ```env
   SUPABASE_URL=https://your-project.supabase.co
   SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
   ```

2. **На Production** - установите те же переменные в Vercel:
   - Vercel Dashboard → Settings → Environment Variables
   - Добавьте для Production

3. **Перезапустите:**
   - Локально: перезапустите `npm run dev`
   - Production: Redeploy в Vercel

4. **Загрузите фотографии заново:**
   - После настройки переменных загрузите фотографии через админ панель
   - Данные сохранятся в Supabase и будут доступны на всех устройствах

## 🚨 Важно!

**Если Supabase не настроен локально:**
- Данные сохраняются в локальный файл `gallery.json`
- Этот файл существует только на вашем компьютере
- Данные НЕ синхронизируются между устройствами
- На production данные будут пустыми

**Для синхронизации ОБЯЗАТЕЛЬНО нужен Supabase:**
- И локально, и на production
- С одинаковыми переменными окружения
- Тогда данные будут синхронизироваться между всеми устройствами

## 📝 Чеклист решения проблемы

- [ ] Проверить переменные окружения локально (`front/.env.local`)
- [ ] Проверить переменные окружения в Vercel (Production)
- [ ] Проверить таблицу `gallery` в Supabase Dashboard
- [ ] Проверить логи при загрузке фотографии
- [ ] Загрузить фотографии заново после настройки Supabase
- [ ] Проверить галерею с другого браузера/устройства

## 🔍 Детальная диагностика

### Шаг 1: Проверьте логи при загрузке

Когда загружаете фотографию, проверьте логи:

**Ожидаемый лог при правильной настройке:**
```
[Gallery] Сохранение галереи: {
  isVercel: false,  // или true на production
  supabaseEnabled: true,  // ДОЛЖНО БЫТЬ true!
  imagesCount: 1,
  hasSupabaseUrl: true,
  hasSupabaseKey: true
}
[Gallery] Используем Supabase для сохранения (синхронизация между устройствами)
[Gallery] Данные успешно сохранены в Supabase
```

**Если видите:**
```
supabaseEnabled: false
```
→ Supabase не настроен, данные сохраняются в локальный файл!

### Шаг 2: Проверьте данные в Supabase

После загрузки фотографии проверьте таблицу:
```sql
SELECT * FROM gallery ORDER BY created_at DESC;
```

Если таблица пуста → данные не сохраняются в Supabase.

### Шаг 3: Проверьте API напрямую

Откройте в браузере:
```
https://rtbtnq.vercel.app/api/gallery
```

Должен вернуться JSON с массивом `images`. Если массив пуст → данных нет в Supabase.

## 💡 Быстрое решение

1. **Создайте `front/.env.local`** с переменными Supabase
2. **Перезапустите локальный сервер**
3. **Загрузите фотографии заново** через админ панель
4. **Проверьте таблицу `gallery`** в Supabase - данные должны появиться
5. **Откройте галерею с другого браузера** - фотографии должны отображаться

