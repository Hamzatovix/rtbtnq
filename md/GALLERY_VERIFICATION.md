# ✅ Проверка системы управления галереей (с Supabase)

## 📋 Структура файлов

### 1. Данные галереи
- ✅ `front/src/data/gallery.json` - JSON файл с данными галереи (для локальной разработки)
  - Формат: `{ "images": [{ "id": string, "src": string, "alt": string }] }`
  - Используется только если Supabase не настроен
- ✅ **Supabase таблица `gallery`** - для production (Vercel)
  - Структура: `id`, `src`, `alt`, `data` (JSONB), `created_at`, `updated_at`
  - Автоматически используется при наличии переменных окружения Supabase

### 2. Сервисный слой
- ✅ `front/src/server/gallery/gallery-json.service.ts` - сервис для работы с галереей
  - Поддержка Supabase для production
  - Fallback на файловую систему для локальной разработки
  - Автоматическое определение источника данных

### 3. API Endpoints
- ✅ `front/src/app/api/gallery/route.ts` - API для работы с галереей
  - **GET** `/api/gallery` - получить все изображения
  - **POST** `/api/gallery` - добавить новое изображение
  - **PUT** `/api/gallery` - обновить галерею (для alt текста)
  - **DELETE** `/api/gallery?id={id}` - удалить изображение

- ✅ `front/src/app/api/upload/image/route.ts` - загрузка изображений
  - Поддерживает параметр `folder` в FormData
  - По умолчанию сохраняет в `products/`
  - Для галереи сохраняет в `gallery/` (Vercel Blob Storage)
  - Валидация: тип image, размер до 5MB

### 4. Страницы
- ✅ `front/src/app/backoffice/gallery/page.tsx` - страница управления галереей
  - Загрузка изображений через файловый input
  - Превью всех изображений в сетке
  - Редактирование alt текста (сохранение при потере фокуса)
  - Удаление изображений с подтверждением
  - Состояния загрузки и обработка ошибок
  - Автоматическое обновление списка после операций

- ✅ `front/src/app/gallery/page.tsx` - публичная страница галереи
  - Читает данные из `/api/gallery`
  - Отображает карусель с изображениями
  - Поддержка свайпов и клавиатурной навигации
  - Обработка пустой галереи и состояния загрузки

### 5. Навигация
- ✅ `front/src/app/backoffice/layout.tsx` - обновлена навигация
  - Добавлен пункт "галерея" в десктопное меню
  - Добавлен пункт "галерея" в мобильное меню

## 🔄 Полный поток загрузки изображения с админ панели

### Шаг 1: Выбор файла
```
Пользователь → Выбирает файл через input → handleFileUpload()
```

### Шаг 2: Валидация файла
```
✅ Проверка типа файла (должен быть image/*)
✅ Проверка размера (максимум 5MB)
❌ Если не проходит → показывается ошибка
```

### Шаг 3: Загрузка файла в Vercel Blob Storage
```
POST /api/upload/image
  ↓
FormData: { file, folder: 'gallery' }
  ↓
Vercel Blob Storage: gallery/{timestamp}-{nanoid}.{ext}
  ↓
Возврат: { url, filename }
```

### Шаг 4: Добавление в галерею
```
POST /api/gallery
  ↓
Body: { src: uploadData.url, alt: filename }
  ↓
loadGallery() → загружает текущие изображения
  ↓
Добавляет новое изображение с nanoid()
  ↓
saveGallery() → сохраняет обновленный список
  ├─ Если Supabase настроен → saveGalleryToSupabase()
  │   └─ Удаляет все записи и вставляет новые
  └─ Если Supabase не настроен → запись в gallery.json
  ↓
Возврат: { id, src, alt }
```

### Шаг 5: Обновление UI
```
loadGallery() → GET /api/gallery → обновление состояния
  ↓
Отображение нового изображения в сетке
```

## 🔍 Проверка функциональности

### ✅ Добавление изображения
1. ✅ Пользователь выбирает файл через input
2. ✅ Файл валидируется (тип image, размер до 5MB)
3. ✅ Файл загружается в Vercel Blob Storage в папку `gallery/`
4. ✅ URL изображения добавляется в галерею через POST запрос
5. ✅ Список обновляется автоматически
6. ✅ **На Vercel**: данные сохраняются в Supabase
7. ✅ **Локально**: данные сохраняются в `gallery.json`

### ✅ Редактирование alt текста
1. ✅ Пользователь редактирует поле alt текста
2. ✅ Изменения сохраняются локально для мгновенной обратной связи
3. ✅ При потере фокуса (onBlur) изменения сохраняются на сервере через PUT запрос
4. ✅ PUT запрос обновляет весь массив изображений
5. ✅ При ошибке сохранения происходит откат к исходным данным

### ✅ Удаление изображения
1. ✅ Пользователь нажимает кнопку удаления (появляется при hover)
2. ✅ Показывается подтверждение через `confirm()`
3. ✅ Изображение удаляется из галереи через DELETE запрос
4. ✅ DELETE запрос фильтрует массив и сохраняет обновленный список
5. ✅ Список обновляется автоматически

### ✅ Отображение на публичной странице
1. ✅ Страница `/gallery` загружает данные из `/api/gallery`
2. ✅ GET запрос использует `loadGallery()` из сервиса
3. ✅ Отображается карусель с изображениями
4. ✅ Поддерживается навигация стрелками и точками
5. ✅ Поддерживается свайп на мобильных устройствах
6. ✅ Обрабатывается состояние загрузки
7. ✅ Обрабатывается пустая галерея

## ✅ Проверка типов и интерфейсов

### TypeScript типы
```typescript
// В gallery-json.service.ts
export interface GalleryImage {
  id: string
  src: string
  alt: string
}

// В backoffice/gallery/page.tsx
type GalleryImage = {
  id: string
  src: string
  alt: string
}
```

- ✅ Все компоненты используют правильные типы
- ✅ API endpoints типизированы
- ✅ Сервисный слой типизирован
- ✅ Нет ошибок линтера

## 🔄 Поток данных (обновленный)

```
Backoffice Gallery Page
  ↓ (загрузка)
GET /api/gallery
  ↓ (loadGallery())
  ├─ Supabase (если настроен) → gallery таблица
  └─ Файл (если Supabase не настроен) → gallery.json
  ↓ (возврат)
Backoffice Gallery Page (отображение)

Backoffice Gallery Page
  ↓ (загрузка файла)
POST /api/upload/image (folder: 'gallery')
  ↓ (сохранение)
Vercel Blob Storage (gallery/filename)
  ↓ (возврат URL)
Backoffice Gallery Page
  ↓ (добавление в галерею)
POST /api/gallery (src, alt)
  ↓ (saveGallery())
  ├─ Supabase (если настроен) → saveGalleryToSupabase()
  └─ Файл (если Supabase не настроен) → gallery.json
  ↓ (обновление)
Backoffice Gallery Page (обновленный список)

Public Gallery Page
  ↓ (загрузка)
GET /api/gallery
  ↓ (loadGallery())
  ├─ Supabase (если настроен) → gallery таблица
  └─ Файл (если Supabase не настроен) → gallery.json
  ↓ (возврат)
Public Gallery Page (карусель)
```

## ✅ Проверка обработки ошибок

### API уровень
- ✅ Обработка ошибок чтения/записи файла
- ✅ Обработка ошибок Supabase (fallback на пустой массив)
- ✅ Валидация входных данных (src обязателен, images должен быть массивом)
- ✅ Возврат понятных сообщений об ошибках
- ✅ Логирование ошибок в консоль

### Frontend уровень
- ✅ Обработка ошибок загрузки файла
- ✅ Обработка ошибок добавления в галерею
- ✅ Отображение сообщений об ошибках пользователю
- ✅ Откат изменений при ошибке сохранения alt текста
- ✅ Fallback на placeholder при ошибке загрузки изображения
- ✅ Сброс состояния загрузки при ошибке

## 🔧 Настройка Supabase (для production)

### Требуемые переменные окружения
```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
SUPABASE_GALLERY_TABLE=gallery  # опционально, по умолчанию 'gallery'
```

### Создание таблицы
Выполните SQL из `front/supabase/migrations/create_gallery_table.sql`:

```sql
CREATE TABLE IF NOT EXISTS gallery (
  id TEXT PRIMARY KEY,
  src TEXT NOT NULL,
  alt TEXT DEFAULT '',
  data JSONB,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_gallery_created_at ON gallery(created_at);
```

### Проверка работы
1. ✅ Локально: работает с файлом `gallery.json` (если Supabase не настроен)
2. ✅ Production: автоматически использует Supabase (если переменные настроены)
3. ✅ Переключение между источниками происходит автоматически

## 🐛 Устранение проблем

### Ошибка: "EROFS: read-only file system" на Vercel
**Причина:** Файловая система на Vercel доступна только для чтения

**Решение:** 
1. Настройте Supabase (см. `md/GALLERY_SUPABASE_SETUP.md`)
2. Установите переменные окружения в Vercel
3. Создайте таблицу `gallery` в Supabase

### Ошибка: "Supabase URL is not configured"
**Решение:** Убедитесь, что `SUPABASE_URL` установлен в переменных окружения

### Ошибка: "401 Unauthorized" при работе с Supabase
**Решение:** Проверьте, что используется `SUPABASE_SERVICE_ROLE_KEY`, а не anon key

### Изображения не сохраняются
**Проверьте:**
1. Таблица `gallery` создана в Supabase
2. Переменные окружения установлены в Vercel
3. Перезапустите приложение после изменения переменных окружения

### Загрузка файла работает, но добавление в галерею не работает
**Проверьте:**
1. API `/api/gallery` POST возвращает успешный ответ
2. Проверьте логи сервера на наличие ошибок
3. Убедитесь, что `saveGallery()` возвращает `true`

## 🎯 Итоговый чеклист

### Базовая функциональность
- [x] JSON файл создан и инициализирован
- [x] API endpoints реализованы (GET, POST, PUT, DELETE)
- [x] Страница управления в backoffice создана
- [x] Публичная страница галереи обновлена для чтения из API
- [x] Навигация обновлена (десктоп и мобильная)
- [x] API загрузки изображений поддерживает папки

### Интеграция Supabase
- [x] Сервисный слой создан с поддержкой Supabase
- [x] Автоматическое переключение между файлом и Supabase
- [x] SQL миграция создана
- [x] Документация по настройке создана

### Типы и качество кода
- [x] Все типы TypeScript корректны
- [x] Нет ошибок линтера
- [x] Обработка ошибок реализована
- [x] Состояния загрузки реализованы
- [x] UX оптимизирован (мгновенная обратная связь, подтверждения)

### Загрузка изображений
- [x] Загрузка файлов работает через Vercel Blob Storage
- [x] Валидация файлов реализована
- [x] Сохранение в галерею работает после загрузки
- [x] Обновление UI после операций работает

## 🚀 Готовность к использованию

**Система полностью готова к использованию!**

Все компоненты работают корректно:
- ✅ Добавление изображений работает (с загрузкой файлов)
- ✅ Редактирование alt текста работает
- ✅ Удаление изображений работает
- ✅ Отображение на публичной странице работает
- ✅ Навигация настроена правильно
- ✅ Обработка ошибок реализована
- ✅ Типы и интерфейсы корректны
- ✅ **Поддержка Supabase для production (Vercel)**
- ✅ **Fallback на файловую систему для локальной разработки**

### Для production на Vercel:
1. Настройте Supabase (см. `md/GALLERY_SUPABASE_SETUP.md`)
2. Установите переменные окружения в Vercel
3. Создайте таблицу `gallery` в Supabase
4. Готово! 🎉

### Для локальной разработки:
- Работает из коробки с файлом `gallery.json`
- Supabase опционален (можно настроить для тестирования)

Можно начинать использовать систему управления галереей! 🎉
