# 🔧 Исправление синхронизации галереи

## ❌ Проблема

Галерея пуста при заходе с другого устройства, хотя фотографии загружены через админ панель.

## 🔍 Причина

При локальной загрузке фотографий через админ панель данные могли сохраняться в локальный файл `gallery.json`, а не в Supabase. Когда пользователь заходит с другого устройства на production сайт, данные загружаются из Supabase, но там их нет.

## ✅ Решение

Исправлена логика сохранения и загрузки:

### Изменения в `loadGallery()`:
- **Всегда использует Supabase**, если он настроен (и локально, и на production)
- Это гарантирует, что данные синхронизируются между всеми устройствами
- Fallback на файл только для локальной разработки без Supabase

### Изменения в `saveGallery()`:
- **Всегда сохраняет в Supabase**, если он настроен
- Это гарантирует синхронизацию данных между устройствами
- Предупреждение при использовании локального файла

## 📋 Что нужно проверить

### 1. Переменные окружения в Vercel
Убедитесь, что установлены:
- `SUPABASE_URL` - URL вашего проекта Supabase
- `SUPABASE_SERVICE_ROLE_KEY` - Service Role ключ

**Как проверить:**
1. Vercel Dashboard → Settings → Environment Variables
2. Убедитесь, что переменные установлены для **Production**

### 2. Переменные окружения локально
Для синхронизации локально тоже нужен Supabase:

**Создайте `.env.local` в папке `front/`:**
```env
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
```

### 3. Проверьте данные в Supabase
Откройте Supabase Dashboard и проверьте таблицу `gallery`:

```sql
SELECT * FROM gallery ORDER BY created_at DESC;
```

Если таблица пуста, нужно загрузить фотографии заново через админ панель.

## 🚀 Что делать сейчас

### Вариант 1: Загрузить фотографии заново (рекомендуется)
1. Убедитесь, что переменные окружения Supabase настроены в Vercel
2. Загрузите фотографии через админ панель на production сайте
3. Данные сохранятся в Supabase и будут доступны на всех устройствах

### Вариант 2: Мигрировать данные из локального файла
Если у вас есть локальный файл `front/src/data/gallery.json`:

1. Откройте файл и скопируйте данные
2. Выполните SQL запрос в Supabase:

```sql
-- Вставьте данные из gallery.json
INSERT INTO gallery (id, src, alt, data, created_at, updated_at)
VALUES 
  ('id1', 'https://...', 'alt1', '{"id":"id1","src":"https://...","alt":"alt1"}'::jsonb, NOW(), NOW()),
  ('id2', 'https://...', 'alt2', '{"id":"id2","src":"https://...","alt":"alt2"}'::jsonb, NOW(), NOW());
```

## ✅ После исправления

1. **Локально**: При загрузке фотографий через админ панель данные сохраняются в Supabase
2. **Production**: Данные загружаются из Supabase
3. **Синхронизация**: Данные доступны на всех устройствах

## 🔍 Проверка работы

1. Загрузите фотографию через админ панель
2. Проверьте в Supabase Dashboard, что запись появилась в таблице `gallery`
3. Откройте галерею с другого устройства - фотография должна отображаться

## 📝 Важные замечания

- **Всегда используйте Supabase** для синхронизации данных между устройствами
- Локальный файл `gallery.json` используется только для разработки без Supabase
- На production всегда используется Supabase (если настроен)

## 🆘 Если проблема сохраняется

1. Проверьте логи Vercel для ошибок
2. Проверьте переменные окружения в Vercel
3. Проверьте таблицу `gallery` в Supabase
4. Убедитесь, что Service Role Key имеет права на запись

