# 🔧 Диагностика проблем с галереей

## ❌ Проблема: Фотографии не отображаются на production

### Симптомы
- Страница показывает "Загрузка галереи..." и не загружает изображения
- Проблема возникает на production (Vercel), но может работать локально

### Возможные причины

#### 1. Проблема с Supabase подключением
**Проверьте:**
- Переменные окружения в Vercel:
  - `SUPABASE_URL` - должен быть установлен
  - `SUPABASE_SERVICE_ROLE_KEY` - должен быть установлен
- Таблица `gallery` существует в Supabase
- В таблице есть данные

**Решение:**
```bash
# Проверьте переменные окружения в Vercel Dashboard
# Settings → Environment Variables
```

#### 2. Проблема с API эндпоинтом
**Проверьте:**
- Откройте в браузере: `https://rtbtnq.vercel.app/api/gallery`
- Должен вернуться JSON с массивом `images`
- Проверьте консоль браузера на ошибки

**Решение:**
- Проверьте логи Vercel для ошибок API
- Убедитесь, что API роут доступен

#### 3. Проблема с CORS или доступом
**Проверьте:**
- Откройте DevTools → Network
- Проверьте запрос к `/api/gallery`
- Проверьте статус ответа и содержимое

**Решение:**
- Убедитесь, что нет блокировки CORS
- Проверьте, что запрос выполняется успешно

#### 4. Проблема с данными в Supabase
**Проверьте:**
- Откройте Supabase Dashboard
- Проверьте таблицу `gallery`
- Убедитесь, что есть записи с полями `src`, `alt`, `id`

**Решение:**
```sql
-- Проверьте данные в таблице
SELECT * FROM gallery ORDER BY created_at ASC;

-- Проверьте структуру таблицы
SELECT column_name, data_type 
FROM information_schema.columns 
WHERE table_name = 'gallery';
```

#### 5. Проблема с форматом данных
**Проверьте:**
- Данные в Supabase должны иметь формат:
  ```json
  {
    "id": "string",
    "src": "https://...",
    "alt": "string"
  }
  ```

**Решение:**
- Убедитесь, что поле `src` содержит полный URL изображения
- Проверьте, что URL изображений доступны

### Шаги диагностики

1. **Проверьте логи Vercel:**
   ```
   Vercel Dashboard → Deployments → [Latest] → Functions → /api/gallery
   ```
   Ищите ошибки в логах

2. **Проверьте API напрямую:**
   ```
   https://rtbtnq.vercel.app/api/gallery
   ```
   Должен вернуть JSON с массивом `images`

3. **Проверьте консоль браузера:**
   - Откройте DevTools → Console
   - Ищите ошибки загрузки
   - Проверьте логи `[Gallery Page]`

4. **Проверьте Network запросы:**
   - DevTools → Network
   - Найдите запрос к `/api/gallery`
   - Проверьте статус и ответ

5. **Проверьте Supabase:**
   - Откройте Supabase Dashboard
   - Проверьте таблицу `gallery`
   - Убедитесь, что данные есть

### Улучшения в коде

Добавлено улучшенное логирование:
- Логи в API роуте для диагностики
- Логи на клиенте для отслеживания загрузки
- Обработка ошибок с детальной информацией

### Быстрое решение

1. **Проверьте переменные окружения в Vercel:**
   ```
   SUPABASE_URL=https://your-project.supabase.co
   SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
   ```

2. **Проверьте таблицу в Supabase:**
   ```sql
   SELECT COUNT(*) FROM gallery;
   SELECT * FROM gallery LIMIT 5;
   ```

3. **Проверьте API:**
   ```
   curl https://rtbtnq.vercel.app/api/gallery
   ```

4. **Перезапустите деплой:**
   - В Vercel Dashboard → Redeploy

### Дополнительная диагностика

Если проблема сохраняется, проверьте:

1. **Логи сервера:**
   - Vercel Dashboard → Logs
   - Ищите ошибки `[Gallery API]` или `[Gallery]`

2. **Проверьте переменные окружения:**
   - Убедитесь, что они установлены для production
   - Проверьте правильность значений

3. **Проверьте права доступа:**
   - Service Role Key должен иметь права на чтение таблицы
   - Таблица должна быть доступна для чтения

4. **Проверьте формат URL изображений:**
   - URL должны быть полными (https://...)
   - URL должны быть доступны публично

### Контакты для поддержки

Если проблема не решается:
1. Проверьте логи Vercel
2. Проверьте логи Supabase
3. Проверьте консоль браузера
4. Создайте issue с деталями ошибки

