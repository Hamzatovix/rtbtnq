# Исправление проблемы с авторизацией галереи

## 🔍 Проблема

Фотографии в галерее были видны только после входа в бекофис. Это происходило потому, что middleware блокировал доступ к `/api/gallery` для неавторизованных пользователей.

## ✅ Решение

Добавлен `/api/gallery` с методом `GET` в список публичных API маршрутов в `middleware.ts`.

### Изменения в `front/src/middleware.ts`:

```typescript
const publicApiRules: Array<{ prefix: string; methods: string[] }> = [
  { prefix: '/api/catalog', methods: ['GET'] },
  { prefix: '/api/products', methods: ['GET'] },
  { prefix: '/api/colors', methods: ['GET'] },
  { prefix: '/api/orders', methods: ['POST'] },
  { prefix: '/api/gallery', methods: ['GET'] }, // ✅ Добавлено
]
```

## 🔒 Безопасность

- ✅ **GET `/api/gallery`** - публичный доступ (все могут просматривать галерею)
- 🔒 **POST `/api/gallery`** - требует авторизацию (только админы могут добавлять фото)
- 🔒 **PUT `/api/gallery`** - требует авторизацию (только админы могут редактировать)
- 🔒 **DELETE `/api/gallery`** - требует авторизацию (только админы могут удалять)

## 📋 Проверка

1. **Откройте галерею без авторизации:**
   ```
   https://rtbtnq.vercel.app/gallery
   ```
   Должны отображаться все фотографии.

2. **Проверьте API endpoint:**
   ```
   https://rtbtnq.vercel.app/api/gallery
   ```
   Должен вернуть JSON с изображениями без ошибки 401.

3. **Проверьте с другого устройства:**
   - Откройте галерею на другом устройстве/браузере
   - Фотографии должны отображаться без необходимости входа

## 🎯 Результат

Теперь галерея доступна всем пользователям для просмотра, но только авторизованные администраторы могут добавлять, редактировать и удалять фотографии.

