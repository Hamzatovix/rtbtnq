# 🎨 RSBTQ Design System Report
## Система дизайна проекта Rosebotanique Store

---

## 📋 Содержание

1. [Цветовая палитра](#цветовая-палитра)
2. [Типографика](#типографика)
3. [Тени (Shadows)](#тени-shadows)
4. [Радиусы скругления](#радиусы-скругления)
5. [Анимации](#анимации)
6. [Кнопки](#кнопки)
7. [Компоненты](#компоненты)
8. [Транзиции и переходы](#транзиции-и-переходы)
9. [Фоновые текстуры](#фоновые-текстуры)
10. [Утилитарные классы](#утилитарные-классы)

---

## 🎨 Цветовая палитра

### Основные брендовые цвета

Проект использует **природную минималистичную** палитру с тёплыми оттенками:

#### **Rose Beige** (Розовый бежевый)
- **HEX**: `#f4efe8`
- **RGB**: `244, 239, 232`
- **HSL**: `34, 35%, 93%`
- **Использование**: Основной фон, тёплые акценты
- **CSS переменная**: `--roseBeige: 244 239 232`

#### **Mist Gray** (Туманный серый)
- **HEX**: `#d6d3ce`
- **RGB**: `214, 211, 206`
- **HSL**: `39, 9%, 82%`
- **Использование**: Границы, разделители, вторичные элементы
- **CSS переменная**: `--mistGray: 214 211 206`

#### **Linen White** (Льняной белый)
- **HEX**: `#f9f9f9`
- **RGB**: `249, 249, 249`
- **HSL**: `0, 0%, 98%`
- **Использование**: Основной фон страниц, карточек
- **CSS переменная**: `--linenWhite: 249 249 249`

#### **Sage Tint** (Оттенок шалфея)
- **HEX**: `#aeb6af`
- **RGB**: `174, 182, 175`
- **HSL**: `125, 8%, 70%`
- **Использование**: Основной акцентный цвет, кнопки, ссылки
- **CSS переменная**: `--sageTint: 174 182 175`

#### **Ink Soft** (Мягкие чернила)
- **HEX**: `#4b4b4b`
- **RGB**: `75, 75, 75`
- **HSL**: `0, 0%, 29%`
- **Использование**: Основной текст, заголовки
- **CSS переменная**: `--inkSoft: 75 75 75`

### Дополнительные цвета

#### Семантические цвета (HSL)
- **Primary**: `160 8% 65%` (зеленоватый акцент)
- **Secondary**: `45 18% 95%` (светлый фон)
- **Muted**: `45 18% 95%` / `240 4% 46%` (приглушённые элементы)
- **Destructive**: `0 84.2% 60.2%` (ошибки, удаление)
- **Border**: `45 15% 88%`
- **Ring** (фокус): `160 8% 65%`

### Применение цветов в Tailwind

```css
// Прямое использование
bg-roseBeige    /* #f4efe8 */
text-mistGray   /* #d6d3ce */
border-sageTint /* #aeb6af */
text-inkSoft    /* #4b4b4b */

// С прозрачностью
bg-sageTint/10      /* 10% прозрачности */
border-mistGray/60  /* 60% прозрачности */
text-inkSoft/60     /* 60% прозрачности (.text-whisper) */
```

---

## ✍️ Типографика

### Шрифты

#### **Display Font**: Cormorant Garamond
- **Тип**: Serif (с засечками)
- **Использование**: Заголовки (h1, h2, h3), акцентный текст, brand name
- **Нагрузка**: Google Fonts
- **Начертания**: 300, 400, 500, 600, 700 (regular, italic)
- **CSS переменная**: `--font-display`, `--font-cormorant`
- **Tailwind класс**: `font-display`, `font-serif`, `.text-graceful`

#### **Body Font**: Inter
- **Тип**: Sans-serif (без засечек)
- **Использование**: Основной текст, UI элементы
- **Нагрузка**: Google Fonts
- **Начертания**: 300, 400, 500, 600, 700
- **CSS переменная**: `--font-body`, `--font-inter`
- **Tailwind класс**: `font-body`, `font-sans`

### Размеры шрифтов (Tailwind)

```typescript
'display-1': 'clamp(3.5rem, 5.2vw + 1rem, 4.25rem)' // Адаптивный заголовок
'display-2': '3rem'                                  // Большой заголовок
'title-1': '2.25rem'                                 // Заголовок секции
'title-2': '2rem'                                    // Подзаголовок
'body': '1.0625rem (17px)'                          // Основной текст
'subtle': '0.9375rem (15px)'                        // Вторичный текст
```

### Стили текста

#### **Text Graceful** (Элегантный текст)
```css
.text-graceful {
  font-family: 'Cormorant Garamond', serif;
  letter-spacing: 0.02em;
  font-weight: 400;
}
```

#### **Text Whisper** (Приглушённый текст)
```css
.text-whisper {
  color: rgba(75, 75, 75, 0.6); /* inkSoft с 60% прозрачности */
}
```

#### **Smallcaps** (Маленькие заглавные)
```css
.smallcaps {
  letter-spacing: .12em;
  text-transform: uppercase;
  font-weight: 500;
}
```

### Высота строк и межбуквенное расстояние

- **Основной текст**: `line-height: 1.8` (body)
- **Заголовки**: `line-height: 0.95-1.10`
- **Letter spacing**: 
  - Обычный: `0.02em` (заголовки)
  - Расширенный: `.12em` (smallcaps), `.18em` (NOAH label)

---

## 🌓 Тени (Shadows)

### Типы теней

#### **Soft Shadow** (Мягкая тень)
```css
--soft-shadow: 0 4px 20px rgba(174, 182, 175, 0.08);
/* Применение */
.shadow-soft
```
- **Использование**: Карточки, лёгкие элементы

#### **Medium Shadow** (Средняя тень)
```css
--medium-shadow: 0 8px 30px rgba(174, 182, 175, 0.15);
/* Применение */
.shadow-medium
```
- **Использование**: Hover состояния, выпадающие меню

#### **Warm Shadow** (Тёплая тень)
```css
--warm-shadow: 0 6px 25px rgba(244, 239, 232, 0.4);
/* Применение */
.shadow-warm
```
- **Использование**: Акцентные элементы, badges

#### **Breathing Shadow** (Дышащая тень)
```css
--breathing-shadow: 0 6px 20px rgba(0, 0, 0, 0.05);
/* Применение */
.shadow-breathing
```
- **Использование**: Общие карточки, контейнеры

#### **Misty Shadow** (Туманная тень)
```css
.shadow-misty {
  box-shadow: 0 8px 32px rgba(174, 182, 175, 0.12);
}
```
- **Использование**: Product cards

### Применение в компонентах

- **Карточки продуктов**: `shadow-misty/50 hover:shadow-misty`
- **Кнопки**: `shadow-breathing`
- **Badges**: `shadow-warm`
- **Header**: `shadow-breathing`

---

## 🔄 Радиусы скругления

### Значения

- **Основной радиус**: `--radius: 1rem (16px)`
- **Tailwind классы**:
  - `rounded-lg`: `1rem`
  - `rounded-md`: `calc(1rem - 2px)`
  - `rounded-sm`: `calc(1rem - 4px)`
  - `rounded-full`: `9999px` (круглые кнопки)
  - `rounded-2xl`: `1.5rem` (большие карточки)
  - `rounded-xl`: `0.75rem`

### Применение

- **Кнопки**: `rounded-full` (полностью круглые)
- **Карточки**: `rounded-2xl` (регулярные), `rounded-lg` (компактные)
- **Изображения**: `rounded-lg`, `rounded-xl`
- **Input поля**: `rounded-full` или `rounded-lg`

---

## 🎬 Анимации

### Ключевые кривые (Easing Functions)

**Основная кривая**: `cubic-bezier(0.22, 1, 0.36, 1)`
- Название: `ease-brand`
- Использование: Все основные переходы

**Альтернативные**:
- `ease-in-out`: Для долгих анимаций
- `ease-out`: Для входов элементов

### Анимации движения

#### **rb-breath** (Дыхание)
```css
@keyframes rb-breath {
  0%, 100% { transform: scale(1); opacity: 0.12; }
  50%     { transform: scale(1.03); opacity: 0.20; }
}
/* Применение */
.anim-breath /* 12s */
.anim-breath-slow /* 22s */
```
- **Длительность**: 12s (быстрое), 22s (Alow)
- **Использование**: Фоновые элементы, декоративные формы

#### **rb-float** (Плавание)
```css
@keyframes rb-float {
  0%, 100% { transform: translateY(0) rotate(0deg); }
  50%     { transform: translateY(-8px) rotate(0.5deg); }
}
/* Применение */
.anim-float /* 22s */
.anim-float-slow /* 18s, 24s */
```
- **Длительность**: 18-24s
- **Использование**: Декоративные элементы

#### **rb-bloom-in** (Раскрытие)
```css
@keyframes rb-bloom-in {
  0%   { transform: scale(.8); opacity: 0; }
  100% { transform: scale(1); opacity: 1; }
}
/* Применение */
.rb-bloom-in /* 600ms или 1.2s */
```
- **Длительность**: 600ms-1.2s
- **Использование**: Появление элементов

#### **rb-breathe** (Мягкое дыхание)
```css
@keyframes rb-breathe {
  0%, 100% { transform: scale(1); opacity: 0.6; }
  50%     { transform: scale(1.05); opacity: 0.8; }
}
/* Применение */
.rb-flower /* 訪問s */
```
- **Длительность**: 4s
- **Использование**: Цветочные декоративные элементы

#### **rb-stitch** (Прошивка)
```css
@keyframes rb-stitch {
  0%   { stroke-dashoffset: 120%; }
  100% { stroke-dashoffset: 0%; }
}
/* Применение */
.anim-stitch-once /* 2.7s */
```
- **Длительность**: 2.7s (однократно)
- **Использование**: SVG линии, декоративные прошивки

#### **rb-koru-breath** (Дыхание кору)
```css
@keyframes rb-koru-breath {
  0%, 100% { transform: scale(1); opacity: .14 }
  50% { transform: scale(1.02); opacity: .20 }
}
/* Применение */
.koru-breath /* 22s */
```
- **Длительность**: 22s
- **Использование**: Спиральные декоративные элементы

#### **rb-saturn-pulse** (Пульсация Сатурна)
```css
@keyframes rb-saturn-pulse {
  0%, 100% { transform: scale(1); opacity: 1; }
  50% { transform: scale(1.08); opacity: 1; }
}
/* Применение */
.saturn-planet /* 24s */
```
- **Длительность**: 24s
- **Использование**: Декоративные планеты

#### **rb-saturn-rings-rotate** (Вращение колец)
```css
@keyframes rb-saturn-rings-rotate {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}
/* Применение */
.saturn-rings-front /* 120s */
.saturn-rings-back /* 140s */
```
- **Длительность**: 120s (передние), 140s (задние)
- **Использование**: Декоративные кольца Сатурна

#### **rb-disc-breath** (Дыхание диска)
```css
@keyframes rb-disc-breath {
  0%, 100% { transform: scale(1) rotateX(0deg) rotateY(-20deg); }
  25% { transform: scale(1.01) rotateX(0deg) rotateY(-18deg); }
  50% { transform: scale(1.02) rotateX(0deg) rotateY(-22deg); }
  75% { transform: scale(1.01) rotateX(0deg) rotateY(-18deg); }
}
/* Применение */
.nord-disc-breath /* 22s */
```
- **Длительность**: 22s
- **Использование**: 3D декоративные диски

#### **rb-nord-sway** (Покачивание)
```css
@keyframes rb-nord-sway {
  0%, 100% { transform: rotate( độngdeg) }
  50% { transform: rotate(0.5deg) }
}
/* Применение */
.nord-sway /* 20s */
```
- **Длительность**: 20s
- **Использование**: Ветви, декоративные элементы

#### **rb-crossfade** (Перекрёстное исчезновение)
```css
@keyframes rb-crossfade {
  0%, 40%   { opacity: 1 }
  50%, 90%  { opacity: 0 }
  100%      { opacity: 1 }
}
/* Применение */
.rb-crossfade--top /* 22s */
```
- **Длительность**: 22s
- **Использование**: Переключение изображений

### Tailwind анимации

```typescript
// Встроенные keyframes
"fade-in": "0% → opacity: 0, translateY(20px)"
"fade-in-up": "0% → opacity: 0, translateY(30px)"
"scale-in": "0% → opacity: 0, scale(0.95)"
"float": "translateY(0) → translateY(-10px)"
"breathing": "scale(1) → scale(1.02)"
"breathing-glow": "opacity 0.3 → 0.6"
```

### Framer Motion анимации

- **Появление карточек**: `initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }}`
- **Hover**: `whileHover={{ y: -5 }}`
- **Transition**: `duration: 0.6, ease: [0.22, 1, 0.36, 1]`

---

## 🔘 Кнопки

### Варианты (Variants)

#### **Primary** (Основная)
```css
/* Стили */
bg-sageTint
text-inkSoft
border-mistGray/60
hover:bg-mistGray/20
rounded-full
```

#### **Ghost** (Прозрачная)
```css
/* Стили */
text-inkSoft
hover:bg-mistGray/20
rounded-full
```

#### **Outline** (Контур органическая)
```css
/* Стили */
text-inkSoft
border-mistGray/60
bg-transparent
hover:bg-mistGray/10
rounded-full
```

### Размеры (Sizes)

```typescript
'sm':   'h-9 px-3'      /* 36px высота */
'md':   'h-10 px-4'     /* 40px высота */
'lg':   'h-11 px-6'     /* 44px высота, по умолчанию */
'icon': 'h-11 w-11'     /* Квадратная кнопка */
```

### Общие свойства кнопок

- **Border radius**: `rounded-full` (полностью круглые)
- **Transition**: `duration-250 ease-brand`
- **Focus ring**: `ring-2 ring-sageTint ring-offset-2`
- **Disabled**: `opacity-50 pointer-events-none`
- **Transform on hover**: `group-hover:scale-110`

---

## 🧩 Компоненты

### Card (Карточка)

```typescript
<Card className="rounded-2xl border border-mistGray/20 shadow-misty/50">
  <CardContent>...</CardContent>
</Card>
```

**Стили**:
- Фон: `white` или `#f9f9f9`
- Граница: `border-mistGray/20`
- Тень: `shadow-misty/50` или `shadow-breathing`
- Радиус: `rounded-2xl` (регулярные), `rounded-lg` (компактные)
- Hover: `hover:shadow-misty`

### Product Card

**Особенности**:
- Изображение: `aspect-square` (компактные) или `aspect-[4/5]` (регулярные)
- Hover эффекты: `group-hover:scale-[1.02]` или `group-hover:scale-103`
- Градиент overlay: `bg-gradient-to-t from-sageTint/10`
- Badges: `backdrop-blur-md shadow-warm`

### Color Swatches (Образцы цветов)

```css
/* Размеры */
'sm': 'w-3.5 h-3.5 sm:w-4 sm:h-4'  /* Компактные */
'lg': PropTypes 'w-5 h-5 sm:w-6 sm:h-6'  /* Регулярные */

/* Стили */
border-radius: 50%  /* Круглые */
border: 2px solid mistGray/30
shadow-warm
transition: transform duration-250
hover:scale-110
checked:ring-2 ring-sageTint
```

### Input (Поля ввода)

- Радиус: `rounded-full` или `rounded-lg`
- Граница: `border-mistGray/60`
- Focus: `ring-2 ring-sageTint`
- Padding: `px-4 py-2` или `px-3 py-1.5`

### Header

- Фон: `bg-white/80 backdrop-breathing`
- Граница: `border-b border-mistGray/30`
- Тень: `shadow-breathing`
- Закрепление: `sticky top-0 z-50`

---

## ⏱️ Транзиции и переходы

### Длительности

```css
duration-250   /* 250ms - быстрые hover */
duration-300   /* 300ms - стандартные */
duration-500   /* 500ms - плавные */
duration-600   /* 600ms - входы */
transition-breathing /* 800ms - длительные */
```

### Типы переходов

#### **Transition Breathing** (Дышащий)
```css
.transition-breathing {
  transition: all 0.8s cubic-bezier(0.22, 1, 0.36, 1);
}
```

#### **Stroke Reveal** (Появление линии)
```css
.stroke-reveal {
  stroke-dasharray: 600;
  stroke-dashoffset: 600;
  transition: stroke-dashoffset 2s cubic-bezier(0.22,1,0.36,1);
}
```

### Области применения

- **Кнопки**: `transition-[transform,background-color] duration-250`
- **Карточки**: `transition-breathing`
- **Ссылки**: `transition-colors duration-500 ease-out`
- **Изображения**: `transition-transform duration-300 ease-brand`

---

## 🎨 Фоновые текстуры

### **bg-texture** (Текстурный фон)
```css
.bg-texture {
  background-image: 
    radial-gradient(circle at 20% 80%, rgba(244, 239, 232, 0.1) 0%, transparent 50%),
    radial-gradient(circle at 80% 20%, rgba(214, 211, 206, 0.1) 0%, transparent 50%),
    radial-gradient(circle at 40% 40%, rgba(174, 182, 175, 0.05) 0%, transparent 50%);
}
```

### **bg-linen** (Льняной фон)
```css
.bg-linen {
  background-image: 
    radial-gradient(circle at 30% 70%, rgba(244, 239, 232, 0.2) 0%, transparent 60%),
    radial-gradient(circle at 70% 30%, rgba(214, 211, 206, 0.15) 0%, transparent 60%);
}
```

### **bg-misty** (Туманный фон)
```css
.bg-misty {
  background-image: 
    radial-gradient(circle at 25% 75%, rgba(244, 239, 232, 0.3) 0%, transparent 50%),
    radial-gradient(circle at 75% 25%, rgba(214, 211, 206, 0.2) 0%, transparent 50%);
}
```

### **backdrop-breathing** (Дышащий фон)
```css
.backdrop-breathing {
  backdrop-filter: blur(6px);
  background: rgba(250, 249, 247, 0.6);
}
```

### **backdrop-misty** (Туманный backdrop)
```css
.backdrop-misty {
  backdrop-filter: blur(8px);
  background: rgba(250, 249, 247, 0.7);
}
```

### **Halo** (Ореол)
```css
.halo {
  background: radial-gradient(
    closest-side,
    rgba(174,182,175,0.14),
    rgba(174,182,175,0.08) 60%,
    transparent 70%
  );
}
```

### **Veil Bottom** (Вуаль снизу)
```css
.veil-bottom {
  background: linear-gradient(to top, rgba(0,0,0,0.06), rgba(0,0,0,0));
}
```

---

## 🛠️ Утилитарные классы

### Отступы

**Контейнеры**:
```typescript
container: {
  padding: {
    DEFAULT: '1rem',
    sm: '1rem',
    md: '2rem',
    lg: '2.5rem'
  }
}
```

### Breakpoints (Точки перелома)

```typescript
sm: '480px'
md: '768px'
lg: '1024px'
xl: '1280px'
```

### Z-index

- `z-0`: Фоновые элементы
- `z-10`: Интерактивные элементы поверх
- `z-20`: Модальные окна, уведомления
- `z-50`: Header, фиксированные элементы

### Декоративные элементы

#### **rb-flower** (Цветок)
- Дыхание при hover: `scale(1.25)`
- Лепестки раскрываются: `rotate(-8deg) scale(.88)` → `rotate(0deg) scale(1.2)`

#### **rb-koru** (Спираль кору)
- Листья растут при hover: `scaleY(0.25)` → `scaleY(1)`
- Stroke reveal: постепенное появление линии

#### **rb-nordic-branch** (Северная ветвь)
- Листья растут: `scaleY(0.25)` → `scaleY(1)`
- Покачивание: `rb-nord-sway 20s`

#### **NOAH stitched label** (Прошитая метка)
- Появление прошивки: `stroke-dashoffset: 240` → `0`
- Текст появляется: `opacity: 0` → `0.9`

### Reduced Motion (Уважение к настройкам)

Все анимации имеют fallback для `prefers-reduced-motion: reduce`:
```css
@media (prefers-reduced-motion: reduce) {
  .anim-breath, .anim-breath-slow, .anim-float { 
    animation: none !important; 
  }
  /* ... другие анимации */
}
```

---

## 📐 Spacing (Отступы)

Система использует стандартную шкалу Tailwind (4px):
- `space-x-1`, `space-y-1`: 4px
- `space-x-2`: 8px
- `space-x-4`: 16px
- `gap-6`: 24px
- `p-6`: 24px тестирования
- `px-12`: 48px
- `px-24`: 96px (контейнеры на больших экранах)

---

## 🎯 Резюме принципов

### Философия дизайна

1. **Природная минималистичность**: Тёплые, естественные цвета
2. **Плавность**: Все переходы используют `cubic-bezier(0.22, 1, 0.36, 1)`
3. **Дыхание**: Множество анимаций создают "живой" интерфейс
4. **Элегантность**: Serif шрифт для заголовков, sans-serif для текста
5. **Мягкость**: Круглые углы, мягкие тени, плавные переходы
6. **Accessibility**: Поддержка reduced motion, контрастные цвета

### Ключевые паттерны

- **Круглые кнопки**: Все кнопки имеют `rounded-full`
- **Hover lift**: Элементы слегка поднимаются при наведении (`y: -5`)
- **Gradient overlays**: Тонкие градиенты на изображениях
- **Backdrop blur**: Размытие фона для модальных окон
- **Color swatches**: Круглые образцы цветов с hover эффектами
- **Breathing animations**: Медленные цикличные анимации для декора

---

## 📚 Дополнительные ресурсы

- **Tailwind Config**: `front/tailwind.config.ts`
- **Global Styles**: `front/src/app/globals.css`
- **Button Component**: `front/src/components/ui/button.tsx`
- **Card Component**: `front/src/components/ui/card.tsx`
- **Product Card**: `front/src/components/product/product-card.tsx`

---

**Версия документа**: 1.0  
**Дата обновления**: 2024  
**Проект**: RSBTQ (Rosebotanique Store)

