# ⚡ Быстрое исправление ошибки EROFS

## ✅ Что исправлено

Код теперь **не пытается записать в файл на Vercel**, если Supabase не настроен. Вместо этого вы получите понятное сообщение об ошибке с инструкциями.

## 🔧 Что нужно сделать

### 1. Настроить Supabase (обязательно для Vercel)

#### Шаг 1: Создать таблицу

В Supabase Dashboard → SQL Editor выполните:

```sql
CREATE TABLE IF NOT EXISTS gallery (
  id TEXT PRIMARY KEY,
  src TEXT NOT NULL,
  alt TEXT DEFAULT '',
  data JSONB,
  created_at TIMESTAMPTZ DEFAULT NOW(),
  updated_at TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_gallery_created_at ON gallery(created_at);
```

#### Шаг 2: Получить ключи

В Supabase Dashboard → Settings → API:
- **Project URL** → `SUPABASE_URL`
- **service_role key** → `SUPABASE_SERVICE_ROLE_KEY`

#### Шаг 3: Установить в Vercel

В Vercel Dashboard → Settings → Environment Variables:

```
SUPABASE_URL=https://ваш-проект.supabase.co
SUPABASE_SERVICE_ROLE_KEY=ваш-service-role-ключ
```

**Важно:** 
- Используйте **service_role** key, не **anon** key!
- Убедитесь, что переменные применены к **Production**

#### Шаг 4: Перезапустить

В Vercel → Deployments → **Redeploy**

## 📋 Что изменилось в коде

1. ✅ Добавлена проверка окружения Vercel
2. ✅ На Vercel код **не пытается** записать в файл
3. ✅ Если Supabase не настроен на Vercel - выводится понятная ошибка
4. ✅ Локально продолжает работать с файлом (если Supabase не настроен)

## 🎯 Результат

После настройки Supabase:
- ✅ Ошибка `EROFS` больше не появится
- ✅ Данные сохраняются в Supabase
- ✅ Галерея работает на Vercel

## 📚 Подробная документация

- `md/GALLERY_SUPABASE_SETUP.md` - полная инструкция по настройке
- `md/ERROR_EROFS_FIX.md` - подробное объяснение ошибки
- `md/GALLERY_CHECK.md` - проверка работы системы

