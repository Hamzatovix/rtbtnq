# 🚀 Быстрая настройка Telegram бота для уведомлений о заказах

## ✅ Шаг 1: Запустите бота

1. Откройте бота в Telegram: https://t.me/rosebotanique_orders_bot
2. Нажмите кнопку **"Start"** или отправьте команду `/start`
3. Бот должен ответить (это важно - бот должен быть "активирован")

---

## 🔑 Шаг 2: Получите токен бота

1. Откройте @BotFather в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота `rosebotanique orders`
4. Нажмите **"API Token"**
5. Скопируйте токен (выглядит примерно так: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

---

## 📱 Шаг 3: Получите Chat ID

### Вариант А: Для личных сообщений (уведомления лично вам)

1. Напишите вашему боту `@rosebotanique_orders_bot` любое сообщение (например, "test")
2. Откройте в браузере (замените `YOUR_BOT_TOKEN` на токен из шага 2):
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```
3. Найдите в ответе `"chat":{"id":123456789}` - это ваш Chat ID

### Вариант Б: Для группы (рекомендуется для команды)

1. Создайте группу в Telegram или используйте существующую
2. Добавьте бота `@rosebotanique_orders_bot` в группу
3. Отправьте любое сообщение в группу
4. Откройте в браузере:
   ```
   https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates
   ```
5. Найдите `"chat":{"id":-1001234567890}` - Chat ID группы (отрицательное число)

**Важно:** Chat ID группы всегда отрицательное число (начинается с минуса)

---

## 📝 Шаг 4: Обновите .env файл

Откройте файл `front/.env` и убедитесь, что там указаны правильные значения:

```env
TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
TELEGRAM_CHAT_ID=ваш_chat_id
NEXT_PUBLIC_BASE_URL=http://localhost:3000
```

**Важно:**
- Токен должен быть БЕЗ кавычек и пробелов
- Chat ID должен быть БЕЗ кавычек
- Для группы Chat ID должен быть отрицательным числом (с минусом)

---

## 🔄 Шаг 5: Перезапустите сервер

1. Остановите сервер (Ctrl+C в терминале)
2. Запустите снова:
   ```bash
   cd front
   npm run dev
   ```

---

## ✅ Шаг 6: Проверьте работу

1. Создайте тестовый заказ через сайт
2. Проверьте консоль сервера - должны появиться логи:
   - `[Order] Заказ создан:`
   - `[Telegram] Проверка конфигурации:`
   - `[Telegram] Сообщение успешно отправлено`

3. Проверьте Telegram - должно прийти уведомление

---

## 🔍 Отладка

Если уведомления не приходят, проверьте консоль сервера:

### Ошибка: "Telegram bot token or chat ID not configured"
→ Переменные окружения не загрузились - перезапустите сервер

### Ошибка: "Bad Request: chat not found"
→ Chat ID неправильный или бот не запущен (напишите боту /start)

### Ошибка: "Unauthorized"
→ Токен бота неправильный - проверьте токен в @BotFather

### Заказ создается, но Telegram ошибок нет
→ Проверьте логи в консоли сервера - там будет подробная информация

---

## 💡 Быстрая проверка токена и Chat ID

Откройте в браузере (замените значения):
```
https://api.telegram.org/botYOUR_TOKEN/sendMessage?chat_id=YOUR_CHAT_ID&text=Тест
```

Если всё правильно, в Telegram придет сообщение "Тест".

