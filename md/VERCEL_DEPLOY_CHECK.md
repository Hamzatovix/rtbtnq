# 🔍 Проверка после деплоя на Vercel

## ✅ У вас уже настроено

- ✅ Supabase база данных существует
- ✅ Таблица `gallery` создана
- ✅ Переменные окружения установлены в Vercel:
  - `SUPABASE_URL`
  - `SUPABASE_SERVICE_ROLE_KEY`

## 🔧 Что нужно сделать

### 1. Проверить структуру таблицы `gallery`

В Supabase Dashboard → Table Editor → `gallery` проверьте, что таблица имеет следующие колонки:

- ✅ `id` (TEXT, PRIMARY KEY)
- ✅ `src` (TEXT, NOT NULL)
- ✅ `alt` (TEXT, может быть пустым)
- ✅ `data` (JSONB, опционально)
- ✅ `created_at` (TIMESTAMPTZ)
- ✅ `updated_at` (TIMESTAMPTZ)

**Если структура отличается**, выполните в SQL Editor:

```sql
-- Проверка структуры
SELECT column_name, data_type, is_nullable
FROM information_schema.columns
WHERE table_name = 'gallery';

-- Если нужно обновить структуру
ALTER TABLE gallery 
ADD COLUMN IF NOT EXISTS src TEXT,
ADD COLUMN IF NOT EXISTS alt TEXT DEFAULT '',
ADD COLUMN IF NOT EXISTS data JSONB,
ADD COLUMN IF NOT EXISTS created_at TIMESTAMPTZ DEFAULT NOW(),
ADD COLUMN IF NOT EXISTS updated_at TIMESTAMPTZ DEFAULT NOW();
```

### 2. Сделать новый деплой на Vercel

Код был обновлен, нужно задеплоить изменения:

**Вариант А: Через Git**
```bash
git add .
git commit -m "Fix: Add Supabase support for gallery"
git push
```

**Вариант Б: Через Vercel Dashboard**
1. Откройте Vercel Dashboard → ваш проект
2. Перейдите в **Deployments**
3. Найдите последний деплой
4. Нажмите **Redeploy** → **Redeploy**

### 3. Проверить логи после деплоя

После деплоя попробуйте добавить изображение в галерею и проверьте логи в Vercel:

**Vercel Dashboard → Deployments → [ваш деплой] → Functions → [функция] → Logs**

Должны появиться логи:
```
[Gallery] Сохранение галереи: {
  isVercel: true,
  supabaseEnabled: true,
  imagesCount: 1,
  hasSupabaseUrl: true,
  hasSupabaseKey: true
}
[Gallery] Используем Supabase для сохранения
```

### 4. Проверить работу

1. Откройте ваше приложение на Vercel
2. Перейдите в `/backoffice/gallery`
3. Попробуйте добавить изображение
4. ✅ Должно работать без ошибки `EROFS`

## 🐛 Если ошибка все еще возникает

### Проверка 1: Переменные окружения

В Vercel Dashboard → Settings → Environment Variables убедитесь:

- ✅ `SUPABASE_URL` установлена для **All Environments** или **Production**
- ✅ `SUPABASE_SERVICE_ROLE_KEY` установлена для **All Environments** или **Production**
- ✅ Значения правильные (без лишних пробелов, кавычек)

### Проверка 2: Структура таблицы

В Supabase Dashboard → Table Editor → `gallery`:

- ✅ Таблица существует
- ✅ Есть колонки: `id`, `src`, `alt`, `data`, `created_at`, `updated_at`
- ✅ Таблица помечена как "Unrestricted" (без RLS) или RLS настроен правильно

### Проверка 3: Логи Vercel

Проверьте логи в Vercel после попытки сохранить:

**Если видите:**
```
[Gallery] Сохранение галереи: {
  isVercel: true,
  supabaseEnabled: false,  ← ПРОБЛЕМА!
  ...
}
```

**Решение:** Переменные окружения не применяются. Проверьте шаг "Проверка 1".

**Если видите:**
```
[Gallery] Используем Supabase для сохранения
Error: 404 Not Found: ...
```

**Решение:** Таблица `gallery` не существует или имеет другое имя. Проверьте шаг "Проверка 2".

**Если видите:**
```
Error: 401 Unauthorized: ...
```

**Решение:** Неправильный `SUPABASE_SERVICE_ROLE_KEY`. Убедитесь, что используете **service_role** key, а не **anon** key.

## 📋 Чеклист

- [ ] Таблица `gallery` существует в Supabase
- [ ] Структура таблицы правильная (id, src, alt, data, created_at, updated_at)
- [ ] Переменные окружения установлены в Vercel
- [ ] Переменные применены к Production окружению
- [ ] Новый деплой выполнен
- [ ] Логи показывают `supabaseEnabled: true`
- [ ] Добавление изображения работает без ошибок

## ✅ После успешной настройки

После того как все заработает:
- ✅ Данные сохраняются в Supabase таблице `gallery`
- ✅ Ошибка `EROFS` больше не появляется
- ✅ Галерея работает на Vercel
- ✅ Можно проверить данные в Supabase Dashboard → Table Editor → `gallery`

