# 🛡️ Защита данных от потери при обновлении

## Проблема

При обновлении проекта через `git pull` локальные изменения в `products.json` и `gallery.json` могут быть потеряны, если они конфликтуют с версией из репозитория.

## Решения

### ✅ Решение 1: Автоматическое резервное копирование (реализовано)

Скрипты `DEPLOY_FULL.sh` и `DEPLOY_QUICK.sh` теперь автоматически:
1. Создают резервную копию данных перед обновлением
2. Сохраняют локальные изменения в git stash
3. Обновляют код из репозитория
4. Восстанавливают данные из stash после обновления

**Использование:**
```bash
cd /opt/rosebotanique
./DEPLOY_FULL.sh    # Полное обновление с бэкапом
./DEPLOY_QUICK.sh   # Быстрое обновление с бэкапом
```

### ✅ Решение 2: Исключение данных из репозитория

Файлы `products.json` и `gallery.json` добавлены в `.gitignore`, чтобы они не попадали в репозиторий.

**Важно:** После этого нужно:
1. Удалить эти файлы из репозитория (но НЕ с сервера!)
2. Создать шаблоны файлов для разработки

### ✅ Решение 3: Ручное резервное копирование

Скрипт `BACKUP_DATA.sh` создает резервную копию данных:

```bash
cd /opt/rosebotanique
chmod +x BACKUP_DATA.sh
./BACKUP_DATA.sh
```

Резервные копии сохраняются в `/opt/rosebotanique/backups/` и автоматически очищаются (остаются только последние 10).

### ✅ Решение 4: Восстановление из резервной копии

Если данные потеряны, можно восстановить из бэкапа:

```bash
cd /opt/rosebotanique
chmod +x RESTORE_BACKUP.sh

# Посмотреть список бэкапов
./RESTORE_BACKUP.sh

# Восстановить последний бэкап
./RESTORE_BACKUP.sh latest

# Восстановить конкретный бэкап (например, номер 1)
./RESTORE_BACKUP.sh 1

# Перезапустить контейнер
docker restart rosebotanique
```

## 📋 Структура резервных копий

```
/opt/rosebotanique/backups/
├── backup_20251220_143022/
│   ├── products.json
│   ├── gallery.json
│   ├── prisma_data/
│   ├── stash_list.txt
│   └── git_status.txt
├── backup_20251220_150145/
│   └── ...
└── ...
```

## 🔄 Рекомендуемый процесс обновления

1. **Автоматически** (рекомендуется):
   ```bash
   ./DEPLOY_FULL.sh
   ```
   Скрипт сам создаст бэкап и восстановит данные.

2. **Вручную** (если нужен контроль):
   ```bash
   # Создать бэкап
   ./BACKUP_DATA.sh
   
   # Обновить код
   git pull
   
   # Перезапустить контейнер
   docker restart rosebotanique
   ```

## ⚠️ Важные замечания

1. **Резервные копии** хранятся на сервере в `/opt/rosebotanique/backups/`
2. **Автоматическая очистка** удаляет старые бэкапы (остаются последние 10)
3. **Git stash** используется как дополнительная защита
4. **Данные монтируются как volume**, поэтому изменения применяются сразу после перезапуска контейнера

## 🆘 Если что-то пошло не так

1. **Посмотреть список бэкапов:**
   ```bash
   ls -lt /opt/rosebotanique/backups/
   ```

2. **Восстановить из бэкапа:**
   ```bash
   ./RESTORE_BACKUP.sh latest
   docker restart rosebotanique
   ```

3. **Восстановить из git stash:**
   ```bash
   git stash list
   git checkout stash@{0} -- front/src/data/products.json
   docker restart rosebotanique
   ```

## 📝 Дополнительные рекомендации

1. **Регулярно проверяйте бэкапы:**
   ```bash
   ls -lt /opt/rosebotanique/backups/ | head -n 6
   ```

2. **Создавайте бэкапы перед важными изменениями:**
   ```bash
   ./BACKUP_DATA.sh
   ```

3. **Рассмотрите использование базы данных** вместо JSON файлов для более надежного хранения данных (Prisma уже настроен)


