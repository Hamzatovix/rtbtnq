# 📱 Как открыть проект на телефоне

## 🚀 Быстрый способ

### Шаг 1: Убедитесь, что телефон и компьютер в одной Wi-Fi сети

- Компьютер и телефон должны быть подключены к одной Wi-Fi сети

### Шаг 2: Узнайте IP-адрес вашего компьютера

**В Windows:**
1. Откройте PowerShell или командную строку
2. Введите команду: `ipconfig`
3. Найдите **IPv4 Address** в разделе **Беспроводная локальная сеть** (Wi-Fi)
4. Запишите этот адрес (например: `192.168.1.100`)

**Альтернативный способ:**
- Нажмите `Win + R`
- Введите `cmd` и нажмите Enter
- Введите `ipconfig` и нажмите Enter
- Найдите IP-адрес в разделе Wi-Fi

### Шаг 3: Запустите проект с доступом из сети

Остановите текущий сервер (если запущен) и запустите:

```bash
cd front
npm run dev:network
```

Или вручную:
```bash
cd front
next dev -H 0.0.0.0
```

### Шаг 4: Откройте на телефоне

В браузере телефона откройте:
```
http://ВАШ_IP_АДРЕС:3000
```

Например: `http://192.168.1.100:3000`

---

## 🔥 Важные замечания

### Для Telegram уведомлений

Если вы хотите, чтобы кнопки в Telegram работали с телефона, нужно:

1. **Обновить .env файл:**
   ```env
   NEXT_PUBLIC_BASE_URL=http://ВАШ_IP_АДРЕС:3000
   ```
   Например: `NEXT_PUBLIC_BASE_URL=http://192.168.1.100:3000`

2. **Перезапустить сервер** после изменения .env

3. **Важно:** Кнопки в Telegram будут работать только если:
   - Ваш IP-адрес доступен из интернета (обычно нет для домашних сетей)
   - Или вы используете ngrok/локальный туннель (см. ниже)

---

## 🌐 Использование ngrok для доступа из интернета

Если нужно, чтобы сайт был доступен из интернета (для Telegram кнопок):

### 1. Установите ngrok
- Скачайте с https://ngrok.com/download
- Или через npm: `npm install -g ngrok`

### 2. Запустите ngrok
```bash
ngrok http 3000
```

### 3. Скопируйте URL (например: `https://abc123.ngrok.io`)

### 4. Обновите .env
```env
NEXT_PUBLIC_BASE_URL=https://abc123.ngrok.io
```

### 5. Перезапустите сервер

Теперь кнопки в Telegram будут работать!

---

## 🔒 Безопасность

⚠️ **Важно:** При использовании `dev:network` или ngrok ваш сайт будет доступен в локальной сети (или интернете). Это нормально для разработки, но не используйте для production данных.

---

## ❓ Решение проблем

### Телефон не может подключиться

1. ✅ Проверьте, что телефон и компьютер в одной Wi-Fi сети
2. ✅ Проверьте IP-адрес компьютера
3. ✅ Убедитесь, что запущен `npm run dev:network` (не просто `npm run dev`)
4. ✅ Проверьте, что брандмауэр Windows не блокирует порт 3000

### Открыть порт в брандмауэре Windows

1. Откройте "Брандмауэр Защитника Windows"
2. Нажмите "Дополнительные параметры"
3. Выберите "Правила для входящих подключений"
4. Создайте новое правило для порта 3000 (TCP)

---

## 📝 Примеры

### Узнать IP-адрес (PowerShell)
```powershell
ipconfig | findstr IPv4
```

### Запустить с доступом из сети
```bash
npm run dev:network
```

### Открыть на телефоне
```
http://192.168.1.100:3000
```

Удачи! 🚀

