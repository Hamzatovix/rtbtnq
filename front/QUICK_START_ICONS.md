# 🚀 Быстрый старт: Генерация иконок

## Проблема с PowerShell и кириллицей

Если у вас проблемы с запуском команды из-за кириллицы в пути, используйте один из способов ниже.

## Способ 1: Запуск из папки front (рекомендуется)

Откройте терминал и выполните:

```powershell
cd front
npm run icons:generate
```

## Способ 2: Прямой запуск скрипта

Из корня проекта:

```powershell
node front\scripts\generate-icons.mjs
```

## Способ 3: Через npm из корня (если есть workspace)

Если проект настроен как workspace, можно добавить скрипт в корневой package.json:

```json
"icons:generate": "cd front && npm run icons:generate"
```

## Что будет создано

После успешного запуска будут созданы файлы:
- `favicon.ico`
- `favicon-16x16.png`
- `favicon-32x32.png`
- `favicon-48x48.png`
- `apple-touch-icon.png`
- `android-chrome-192x192.png`
- `android-chrome-512x512.png`
- `icon-192x192.png`
- `icon-512x512.png`

## Проверка

После генерации:
1. Перезапустите Next.js сервер
2. Очистите кеш браузера
3. Проверьте favicon во вкладке

