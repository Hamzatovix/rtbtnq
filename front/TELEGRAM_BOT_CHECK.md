# Проверка работы Telegram бота

## 📋 Текущая конфигурация

### Где используется:
- **Файл:** `front/src/lib/telegram.ts`
- **Вызывается из:** `front/src/server/orders/orders-json.service.ts` (при создании заказа)
- **Функция:** `sendOrderNotification()`

### Требуемые переменные окружения:
- `TELEGRAM_BOT_TOKEN` - токен бота от @BotFather
- `TELEGRAM_CHAT_ID` - ID чата для получения уведомлений

## ✅ Как проверить работу бота

### Способ 1: Проверка через создание тестового заказа

1. Откройте сайт: `https://rosebotanique.store` (или локально)
2. Добавьте товар в корзину
3. Перейдите на страницу оформления заказа (`/checkout`)
4. Заполните форму и создайте заказ
5. Проверьте Telegram - должно прийти уведомление

### Способ 2: Проверка через логи сервера

На сервере проверьте логи Docker контейнера:
```bash
docker logs rosebotanique --tail 100 | grep Telegram
```

Должны увидеть:
- `[Telegram] Проверка конфигурации:` - проверка токена и chat ID
- `[Telegram] Сообщение успешно отправлено` - успешная отправка
- Или ошибки, если что-то не настроено

### Способ 3: Проверка переменных окружения на сервере

```bash
# На сервере
docker exec rosebotanique env | grep TELEGRAM
```

Должны увидеть:
- `TELEGRAM_BOT_TOKEN=...`
- `TELEGRAM_CHAT_ID=...`

## 🔍 Что проверить

### 1. Переменные окружения установлены:
- ✅ `TELEGRAM_BOT_TOKEN` - должен быть установлен
- ✅ `TELEGRAM_CHAT_ID` - должен быть установлен

### 2. Формат сообщения:
Сообщение должно содержать:
- ✨ Заголовок "НОВЫЙ ЗАКАЗ"
- 📦 Номер заказа
- 👤 Имя клиента
- 📞 Телефон
- 🛒 Список товаров с ценами
- 💰 Итоговая сумма
- 🚚 Способ доставки (без цены, если цена = 0)
- 📍 Адрес (если указан)
- 💬 Комментарий (если есть)
- 🔗 Кнопка "Посмотреть заказ" (если не localhost)

### 3. Форматирование:
- Используется Markdown форматирование
- Разделители `━━━━━━━━━━━━━━━━━━━━`
- Эмодзи для визуального разделения

## ❌ Возможные проблемы

### Проблема: Уведомления не приходят

**Причины:**
1. Переменные окружения не установлены
2. Неправильный токен бота
3. Неправильный Chat ID
4. Бот заблокирован или удален
5. Проблемы с сетью на сервере

**Решение:**
1. Проверьте переменные окружения на сервере
2. Проверьте токен через @BotFather
3. Проверьте Chat ID через @userinfobot
4. Проверьте логи сервера на ошибки

### Проблема: Сообщения приходят, но форматирование неправильное

**Причина:** Проблема с Markdown форматированием

**Решение:** Проверьте функцию `formatOrderNotification()` в `telegram.ts`

## 📝 Пример правильного сообщения:

```
✨ НОВЫЙ ЗАКАЗ
━━━━━━━━━━━━━━━━━━━━

📦 Заказ: `RB-20251208-ABC123`
👤 Клиент: Иван Иванов
📞 Телефон: +7 (999) 123-45-67

━━━━━━━━━━━━━━━━━━━━

🛒 ТОВАРЫ

1. *Роза красная* • Красный
   2 шт. × 500 ₽ = 1 000 ₽

━━━━━━━━━━━━━━━━━━━━

💰 ИТОГО: 1 000 ₽

🚚 Доставка: Ozon доставка

📍 Адрес:
Россия, Москва, ул. Тестовая, д. 1
```

## 🔧 Настройка на сервере

Если переменные окружения не установлены, добавьте их в `.env.production` или в Docker:

```bash
# В Dockerfile или docker-compose.yml
ENV TELEGRAM_BOT_TOKEN=ваш_токен
ENV TELEGRAM_CHAT_ID=ваш_chat_id
```

Или через переменные окружения при запуске:
```bash
docker run -d \
  -e TELEGRAM_BOT_TOKEN=ваш_токен \
  -e TELEGRAM_CHAT_ID=ваш_chat_id \
  ...
```

