# 📱 Инструкция по генерации иконок

## Быстрый запуск

```bash
npm run icons:generate
```

Или напрямую:

```bash
node scripts/generate-icons.mjs
```

## Что делает скрипт

Скрипт `scripts/generate-icons.mjs` генерирует все необходимые иконки из файла `front/public/icon-source.PNG`:

1. **Обрезает белые поля** (trim) из исходного изображения
2. **Добавляет безопасную зону** (safe-area) для читаемости на маленьких размерах
3. **Создает все необходимые размеры**:
   - `favicon.ico` (содержит 16/32/48px)
   - `favicon-16x16.png`
   - `favicon-32x32.png`
   - `favicon-48x48.png`
   - `apple-touch-icon.png` (180x180 для iOS)
   - `android-chrome-192x192.png` (для Android PWA)
   - `android-chrome-512x512.png` (для Android PWA высокого разрешения)
   - `icon-192x192.png` (для обратной совместимости)
   - `icon-512x512.png` (для обратной совместимости)
   - `site.webmanifest` (PWA манифест)

## Требования

- Node.js 18+
- Библиотека `sharp` (уже в devDependencies)

## После генерации

1. Перезапустите Next.js сервер (`npm run dev` или `npm run build`)
2. Очистите кеш браузера (Ctrl+Shift+Delete)
3. На мобильных устройствах переустановите PWA или очистите кеш приложения

## Проверка

После генерации проверьте:
- ✅ Favicon отображается во вкладке браузера
- ✅ Иконка видна в закладках
- ✅ На iOS при добавлении на главный экран используется правильная иконка
- ✅ На Android PWA иконки корректны

