# Объяснение использования иконок в проекте Rosebotanique

## 📁 Расположение файлов иконок

Все иконки находятся в директории `front/public/`:

### Основные иконки:
- `/favicon.svg` - SVG favicon для современных браузеров
- `/favicon.ico` - ICO favicon для старых браузеров (fallback)
- `/apple-touch-icon.png` - Иконка для iOS устройств (180x180)
- `/icon.svg` - Альтернативная иконка (не используется напрямую)
- `/logo/logo_day.svg` - Логотип бренда для хедера

---

## 🎯 Где используются иконки

### 1. **favicon.svg** (`/favicon.svg`)
**Использование:**
- ✅ Основной favicon в браузере (современные браузеры)
- ✅ Используется в `<head>` через `metadata.icons.icon[0]`
- ✅ Используется в `manifest.json` как основная иконка PWA
- ✅ Используется в `metadata.icons.shortcut`

**Где настроено:**
- `front/src/app/layout.tsx` (строки 68, 76)
- `front/public/manifest.json` (строка 12)

**Что это:**
- Копия `logo_day.svg` - логотип бренда Rosebotanique
- SVG формат для масштабирования без потери качества
- Отображается во вкладке браузера

---

### 2. **favicon.ico** (`/favicon.ico`)
**Использование:**
- ✅ Fallback для старых браузеров
- ✅ Используется в `<head>` через `metadata.icons.icon[1]`
- ✅ Используется в `manifest.json` как дополнительная иконка

**Где настроено:**
- `front/src/app/layout.tsx` (строка 69)
- `front/public/manifest.json` (строка 18)

**Что это:**
- ICO формат (32x32 PNG в формате ICO)
- Генерируется автоматически из `logo_day.svg` через скрипт `generate-favicon.js`
- Отображается во вкладке браузера в старых браузерах

---

### 3. **apple-touch-icon.png** (`/apple-touch-icon.png`)
**Использование:**
- ✅ Иконка для iOS устройств (iPhone, iPad)
- ✅ Используется при добавлении сайта на главный экран iOS
- ✅ Используется в `<head>` через `metadata.icons.apple` и `metadata.icons.icon[2]`
- ✅ Используется в `manifest.json` для PWA

**Где настроено:**
- `front/src/app/layout.tsx` (строки 70, 73)
- `front/public/manifest.json` (строка 23)

**Что это:**
- PNG формат, размер 180x180 пикселей
- Генерируется автоматически из `logo_day.svg` через скрипт `generate-favicon.js`
- Отображается на главном экране iOS устройств

---

### 4. **logo_day.svg** (`/logo/logo_day.svg`)
**Использование:**
- ✅ Основной логотип бренда в хедере сайта
- ✅ Используется в компоненте `Logo` (`front/src/components/ui/logo.tsx`)
- ✅ Используется как источник для генерации favicon файлов
- ✅ Используется в Structured Data (Organization, LocalBusiness) как `logo`

**Где настроено:**
- `front/src/components/ui/logo.tsx` (строка 34)
- `front/src/components/seo/OrganizationStructuredData.tsx` (строка 12)
- `front/src/components/seo/LocalBusinessStructuredData.tsx` (строка 15)
- `front/scripts/generate-favicon.js` (строка 30) - как источник для генерации

**Что это:**
- SVG логотип бренда Rosebotanique
- Отображается в хедере сайта вместе с текстом "ROSEBOTANIQUE"
- Используется для генерации всех favicon файлов

---

### 5. **icon.svg** (`/icon.svg`)
**Использование:**
- ⚠️ **В настоящее время НЕ используется напрямую**
- Может использоваться Next.js автоматически (если находится в `app/icon.svg`)
- Содержит упрощенную версию Elven Flower

**Где может использоваться:**
- Next.js автоматически подхватывает `app/icon.svg` если он существует
- В нашем случае мы используем явную настройку через `metadata.icons`

**Что это:**
- Альтернативная иконка с упрощенным дизайном
- Не используется в текущей конфигурации

---

## 🔧 Как генерируются иконки

### Автоматическая генерация:
Все favicon файлы генерируются автоматически из `logo_day.svg` через скрипт:

**Скрипт:** `front/scripts/generate-favicon.js`

**Команда для запуска:**
```bash
npm run favicon:generate
```

**Что генерируется:**
1. `favicon.ico` (32x32) - из `logo_day.svg`
2. `apple-touch-icon.png` (180x180) - из `logo_day.svg`
3. `favicon.svg` - копия `logo_day.svg`

**Библиотека:** Используется `sharp` для конвертации SVG в PNG/ICO

---

## 📱 Где отображаются иконки

### В браузере:
- **Вкладка браузера** - `favicon.svg` или `favicon.ico`
- **Закладки** - `favicon.svg` или `favicon.ico`
- **История браузера** - `favicon.svg` или `favicon.ico`

### На мобильных устройствах:
- **iOS (iPhone/iPad)** - `apple-touch-icon.png` при добавлении на главный экран
- **Android** - иконки из `manifest.json` при установке PWA

### В поисковых системах:
- **Google Search** - использует `favicon.svg` или `favicon.ico` в результатах поиска
- **Open Graph** - использует изображения из `metadata.openGraph.images`

### В социальных сетях:
- **Facebook, Twitter** - используют Open Graph изображения
- **WhatsApp, Telegram** - используют Open Graph изображения

---

## 🎨 Структура иконок в коде

### В `layout.tsx` (metadata):
```typescript
icons: {
  icon: [
    { url: '/favicon.svg', type: 'image/svg+xml' },      // Основной favicon
    { url: '/favicon.ico', sizes: 'any' },                // Fallback для старых браузеров
    { url: '/apple-touch-icon.png', type: 'image/png', sizes: '180x180' }, // iOS
  ],
  apple: [
    { url: '/apple-touch-icon.png', sizes: '180x180', type: 'image/png' },
  ],
  shortcut: [
    { url: '/favicon.svg', type: 'image/svg+xml' },
  ],
}
```

### В `manifest.json`:
```json
{
  "icons": [
    { "src": "/favicon.svg", "sizes": "any", "type": "image/svg+xml" },
    { "src": "/favicon.ico", "sizes": "48x48", "type": "image/x-icon" },
    { "src": "/apple-touch-icon.png", "sizes": "180x180", "type": "image/png" }
  ]
}
```

---

## 📋 Итоговая таблица использования

| Файл | Где используется | Назначение |
|------|------------------|------------|
| `favicon.svg` | Браузер, PWA, метаданные | Основной favicon для современных браузеров |
| `favicon.ico` | Браузер (старые), PWA | Fallback для старых браузеров |
| `apple-touch-icon.png` | iOS устройства, PWA | Иконка для главного экрана iOS |
| `logo_day.svg` | Хедер сайта, SEO | Логотип бренда в интерфейсе |
| `icon.svg` | Не используется | Альтернативная иконка (резерв) |

---

## 🔄 Как обновить иконки

1. **Обновите исходный логотип:**
   - Отредактируйте `/public/logo/logo_day.svg`

2. **Перегенерируйте favicon файлы:**
   ```bash
   npm run favicon:generate
   ```

3. **Проверьте результат:**
   - Откройте сайт в браузере
   - Проверьте вкладку браузера
   - Проверьте на мобильном устройстве

---

## ✅ Рекомендации

1. **Всегда используйте SVG** для favicon (лучшее качество)
2. **Сохраняйте ICO** для совместимости со старыми браузерами
3. **Обновляйте apple-touch-icon** при изменении логотипа
4. **Проверяйте иконки** в разных браузерах и устройствах
5. **Используйте один источник** (`logo_day.svg`) для всех иконок

